# Chronos 技术建议(2026-08,基于 2.2.0+25 代码评审)

> 立场:不改设计初衷(浅蓝极简 / 无 emoji / 纯本地优先),只从技术可行性与收益排序给出建议。
> 状态图例:✅ 已落地(2.2.0) · ⏳ 建议下一批 · 🚫 按用户要求跳过/暂缓。
> 用户指示:**更新源机制不动**(latest.json/更新检查/下载/明文流量配置保持原样)。

---

## 一、网络层统一与可观测 ✅(2.2.0 落地)

1. ✅ **HttpJson 统一客户端**:`lib/core/services/http_json.dart` 收拢 eleven/tavily/weather/llm 的 HTTP 请求——统一连接/读写超时、非 2xx 错误体透出(带脱敏日志)、连接复用、SSE 流式接口(`postSse` 返回 Stream,订阅方取消即中断)。**更新源(app_info/content_updater)按约定保持独立,未经过这里。**
2. 🚫 **HTTPS 化/收窄明文**:更新源与 Nocturne 均为用户可配的 http 地址,收窄 `usesCleartextTraffic` 会破坏它们——按用户指示不动。
3. ✅ **日志脱敏**:`log_sanitize.dart` 统一打码 API Key / Bearer / token / 查询参数,错误响应体落日志前过一遍。

## 二、聊天体验(I/O 编排)✅(2.2.0 大部分落地)

1. ✅ **流式输出(SSE)**:`LlmService.chatStream` 逐 token 产出,闲话铺逐字上屏,首字秒出;支持工具调用(流内执行后继续)与 400/422 降级重试。
2. ✅ **停止生成与生命周期**:生成中按钮变「停止」,可保留已生成部分;离开页面/切换会话自动取消 in-flight 请求(`StreamSubscription` 句柄管理)。
3. ✅ **图片持久化(DB v14)**:发图复制到 `chat_imgs/` 并随 `chat_messages.image_path` 落库,历史消息显示原图(旧数据占位);备份 zip 打包/恢复纳入聊天图片;迁移三件套齐全(迁移代码 + 迁移测试 + 升级前备份)。
4. ✅ **上下文压缩**:超过 20 条时用快速模型压缩早期对话(摘要注入,带缓存,失败回退硬裁剪)。
5. ⏳ **工具调用解耦**:确认弹窗与执行仍耦合在 `chat_page._execTool`,可进一步抽「工具执行服务」便于单测;`web_search`/记忆提炼并发上限暂未做(当前并发规模下风险低)。

## 三、日志与诊断 ✅(2.2.0 落地)

1. ✅ 启动日志新增「各 API 是否已配置」布尔摘要(不含密钥),排障第一眼定位配置问题。
2. ✅ 设置页新增「导出诊断包」(db + app_log.txt + preferences.json,一键打包并唤起分享)。
3. ✅ 聊天全链路埋点(2.1.0)+ 错误即时落盘(2.1.0)+ 启动配置摘要(2.2.0)。
4. ⏳ 日志按天滚动文件:当前 256KB 截断「丢旧留新」对排错够用,后续需分析历史时再改。

## 四、数据层 ✅(2.2.0 部分落地)

1. ✅ **memories 去重唯一索引(DB v14)**:迁移时清理历史重复(kind+content 留最早一条)+ 唯一索引,`MemoryService.add` 保留去重查询并加唯一索引兜底(极端重复返回 0)。
2. ✅ **聊天会话清空(可撤销)**:会话记录页「清空记录」一键清空该会话消息,提示条可撤销恢复(原 id 插回)。
3. ✅ **自动备份开关**:设置页开启后,启动时距上次备份 ≥7 天自动静默导出一份(后台执行,不影响启动)。
4. ⏳ 记账/聊天高频表按月治理:明细已分批渲染,会话可清空;若长期增长可再加「归档会话」。

## 五、安全 ✅(2.2.0 落地)

1. ✅ **密钥进 Keystore**:`KeyStore` 底层改 `flutter_secure_storage`(Android Keystore 加密),API 不变、调用点零改动;旧版 SharedPreferences(`api_` 前缀)密钥首次读取自动迁移并删除旧值;备份导出仍排除密钥。
2. 🚫 **更新源防篡改**(latest.json 签名 / HTTPS / APK SHA-256 校验):按用户指示不动。
3. ✅ **外置记忆 token 等**:全部 KeyStore 键已随 1 一并进 Keystore。

## 六、架构与工程 ✅(2.2.0 部分落地)

1. ✅ **共享三态组件**:`status_views.dart`(Loading/Error/Empty)下沉,闲话铺/会话列表/记账已接入。
2. ✅ **提交防重下沉**:`AppTextField` 新增 `submitLocked`,生成中锁定聊天提交;长文本页(日记/速记)原有按钮禁用语义不变。
3. ✅ **CI 工作流**:`.github/workflows/ci.yml`(Windows runner:pub get → analyze → test)。
4. 🚫 **状态管理 riverpod / go_router / 深色随色板**:用户确认三项条件性建议本次跳过,模块规模上来后再评估。

## 七、性能与 UI 细节 ✅(2.2.0 部分落地)

1. ✅ **聊天列表懒加载**:`ListView.builder` 替代一次性构建全部消息(长会话不再整表构建)。
2. ⏳ 图片解码缓存:头像/背景图/聊天图均为小尺寸,Flutter 自带 ImageCache 已覆盖;如后续做大图预览再上 LRU。
3. 🚫 深色随色板:用户确认跳过。

## 八、产品/体验 ✅(2.2.0 落地)

1. ✅ 闲话铺首次进入一次性引导(「会话记录在右上角」)。
2. ✅ 记账点击可编辑 + 左滑可删除(2.1.0),纠错闭环完整。
3. ✅ 掌柜设置记忆入口补充外置记忆(Nocturne)配置说明。

---

> 下一批建议:**工具执行服务解耦、日志按天滚动、记账/聊天表长期治理、图片大图预览缓存**;
> 若用户启用 https 更新源 + Nocturne,再评估收窄明文流量与 latest.json 签名(当前按指示不动)。
