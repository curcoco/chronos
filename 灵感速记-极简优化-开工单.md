# 学生学习工作台 — 灵感速记 · 极简优化(参考 iCity 的"快速记录"思路)

> 在**已完成的灵感速记页**基础上做一轮「极简 + 顺手动线」打磨,**不重写,不新增复杂功能**。
> 项目在 `D:\dev\student_workbench`。目标版本号 **1.4.0+1**(见下方「版本号」)。
>
> ⚠️ 重要背景:
> 1. 灵感速记页已存在且能用 —— `lib/pages/quick_note_page.dart`(autofocus 输入框 + 保存 + 下方「灵感专区」列表),
>    入口已有:底部 **+** 号直达(`main_shell.dart`)、首页仪表盘快捷输入(`home_page.dart`)、侧边栏。
> 2. 本次只做「**界面极简 + 记录动线更顺**」,**不加**标签分类、心情转盘、情绪曲线、语音/图片、云同步等——
>    那些属于后续批次,不是这次的事。**保持纯本地、零网络**(这是本项目铁律)。

## 设计基准:参考 iCity 的什么

用户看完 iCity 之后,明确要参考它的这一块:
> **A. 极简的界面气质 + 快速记录动线**(打开 → 顺手写完 → 走人,不被打扰)。

参考的不是它的功能堆砌,而是那种「记完就走、清爽不啰嗦」的感觉:
- 打开就能写(不用找按钮、不用等加载)。
- 写一句灵感 → 一个动作存下 → 心里不惦记。
- 界面干净,不堆图标不堆装饰,专注文字本身。

## 要做的事(按此顺序)

### A. 记录动线更顺(`quick_note_page.dart`)
1. **保存动作够大够近**:把「保存」做成**键盘右下角的显眼按钮**或**底部通栏大按钮**(拇指好够到),不依赖 AppBar 右侧小按钮。
   - 如果当前已经是 `FilledButton`,改成 `FilledButton.icon` 或加大尺寸、放到更顺手的位置。
2. **顺手就能存**:支持**键盘「完成/回车」直接保存**(配合 `TextInputAction.done` 或监听提交),少一次点击。
3. 保存后轻反馈:保留现有 `SnackBar('已保存到灵感专区 ✨')`,但**输入框保持聚焦/清空**,方便连写多条。
4. 空内容不保存的校验保留(现在的 `_save` 已有,别删)。

### B. 界面极简(`quick_note_page.dart` 视觉)
1. **去装饰**:列表卡片太"满"——当前每张卡有个灯泡图标 + 卡片式边框。改成**更素的时间线列表**:
   - 去掉每张的图标(或只保留很轻的),减少视觉噪声。
   - 卡片 → 细分割线分隔的纯文本列表,或保留极淡的卡片,整体留白更多。
   - 日期时间标签保留(用户需要知道何时记的),但字号/颜色更弱。
2. **专注文字**:正文是主角,字号/行距给足,别让按钮和图标抢注意力。
3. 顶部「✨ 灵感专区」标题行可以简化(去掉 emoji 或保留均可,以"清爽"为准)。

### C. 入口一致性(可选,低优先)
- 首页仪表盘快捷输入、底部 + 号、侧边栏三处入口**行为保持一致**(都是"打开能写"),确认没有一处点了没反应或体验断档。若都正常,此项可只核对不动。

## 版本号

- `pubspec.yaml` 升到 **`1.4.0+1`**。
- `lib/services/app_info.dart` 里 `AppInfo.latestVersion` 离线兜底同步到 **`1.4.0`**。

## 产出与同步(生成文件即可,上传由小言/我负责)

- 打包新 APK,复制为 **`releases/chronos-1.4.0.apk`**。
- 更新 **`latest.json`**:`version`=`1.4.0`、`note`=本次更新说明(灵感速记更极简顺手)、`apk`=
  `https://chronos-update-1456265383.cos.ap-guangzhou.myqcloud.com/releases/chronos-1.4.0.apk`。
- **不需要你 push 或上传**,文件就位即可(小言这边负责上传 COS + 推 GitHub 备份)。

> ⚠️ **COS 对 APK 的已知限制(重要,别当 bug 去查)**:
> 腾讯云 COS **默认域名** `*.cos.ap-guangzhou.myqcloud.com` **禁止公网分发 APK/IPA 安装包**,
> 访问返回 `403 DownloadForbidden`,报错原文:
> `The APK/IPA file is not allowed to be distributed in a public network using COS default domain, please use custom domain instead.`
> (`latest.json` 能读 200,但 `releases/*.apk` 一律 403,已验证。)
> 这不是配置错误——**要公网分发 APK 必须配自定义域名**(需已备案域名 + CDN),此路未走通前,
> **1.4.0 的 APK 由小言手动发给朋友**,不要指望 COS 公网链接。latest.json 仍照常更新,留待以后配好域名后用。

## 技术约束

- 沿用现有架构;**这是本地单机功能,零网络、零权限新增**。别加网络库、别动 manifest 权限。
- 不要碰数据表结构(`note.dart`/`note_service.dart`/`db_helper.dart` 保持不变),只做 UI 与交互层。
- INTERNET 权限已在 manifest(那是更新用的,别删,但本功能不碰网络)。

## 验收标准

- `flutter analyze` 无报错。
- 打开灵感速记页:**打开即能写**,键盘「完成」或保存按钮都能存下,存完清空可连写。
- 列表更素、留白更多,一眼能读完文字;删除入口仍在(可删无用灵感)。
- 首页 / + 号 / 侧边栏三处入口都能进到同一页。
- 打包出 `1.4.0` APK,`latest.json` 与 `releases/chronos-1.4.0.apk` 就位。
- 旧数据(已存的灵感)更新后**不丢**。
