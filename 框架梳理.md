# 学生学习工作台 — 框架梳理

> 依据 `需求文档.md`(全 14 模块总蓝图)+ 当前代码现状,整理整体框架。
> 版本基线:**1.7.1+18**(2026-08-17,灵感速记换行框、计划左滑、删除确认统一、从 zip 恢复+自动重启;compileSdk 36)。纯本地优先,联网功能单列。
> 上手与发版流程见 `交接文档.md`。

---

## 一、总体定位

- 装到安卓真机的学生日常学习生活工作台(Flutter 打包 APK)。
- **离线优先**:计划 / 金币 / 灵感 / 记账等纯功能走本地存储;仅天气、AI 聊天等需外部服务。
- **分期交付**:先 MVP 骨架,再按批次补齐模块(见「路线图」)。

## 二、技术栈与分层

| 层 | 内容 |
|---|---|
| UI | `lib/pages/`(页面)、`lib/widgets/`(复用组件:SectionCard、AddTaskSheet、RandomTaskSheet、MoodBadge、FrostedSnack、**AppTextField**、**ConfirmDialog**) |
| 服务 | `lib/services/`:db_helper(sqflite 单例)、**base_dao(泛型 CRUD 基类)**、task_service、note_service、diary_service、wish_service、coin_service、review_service、settings_service、backup_service、app_info,以及联网类 llm_service / eleven_service / weather_service / supabase_sync_service |
| 数据 | `lib/data/`:离线内容池(金句/每日英语/自动任务/英语/健康/心愿,按日期种子确定性选取) |
| 模型 | `lib/models/`:StudentTask、Note、Wish、CoinRecord、DiaryEntry、Review、EnglishFav、LedgerTxn、KitchenItem、Workout、MemoryItem、PlanItem 等 |
| 工具 | `lib/utils/dates.dart`(日期格式化)、`lib/theme.dart`(浅蓝 Material 3,明暗双主题) |
| 配置 | `lib/config/api_config.dart`(仅默认人设,无密钥)、`lib/services/key_store.dart`(应用内填写的 key,存 SharedPreferences) |

### 共享组件约定(1.7.0 起,新代码必须复用,不要各页手写)

- `widgets/app_text_field.dart` `AppTextField` — 所有文本输入框。`onSubmit` 启用「完成=提交」并带中文输入法换行兜底;长文本编辑传 `submitOnEnter: false`。
- `widgets/confirm_dialog.dart` `showConfirmDialog(...)` — 所有确认弹窗;删除类传 `destructive: true`。
- `widgets/frosted_snack.dart` `showFrostedSnack(...)` / `showUndoSnack(...)` — 普通提示 / 可撤销的删除·清空。
- `services/base_dao.dart` `BaseDao<T>` — 「单例 + 简单 CRUD」的服务继承它(note/diary/wish 已改);复杂聚合(coin/plan)仍各自实现。

## 三、导航框架(当前实现)

```
SplashPage(启动页)
 ├─ 「开始今天」→ MainShell
 └─ 「快速记一笔」→ QuickNotePage(灵感速记,直达)

MainShell(主框架)
 ├─ 底部 5 Tab:首页 / 计划 / +号(大一圈·浮起) / 知识 / 生活
 │    ├─ 首页:任务概览 · 灵感速记框 · 每日英语一句 · 金币卡 · 菜单按钮
 │    ├─ 计划:今日清单(手写添加 / 🎲随机任务)· 本周计划(占位)· 长期目标(占位)
 │    ├─ 知识 / 生活:静态占位页
 │    └─ +号 → QuickNotePage
 └─ 左侧抽屉(Drawer):应用信息 · 版本与更新卡片 · 导航快捷 · 系统设置入口
      └─ 系统设置页:关于 + 当前/最新版本 + 更新状态
```

- 首页/计划页右上角菜单按钮开抽屉(设置等次级入口已从首页移入抽屉)。
- 快捷灵感入口共 3 处:启动页「快速记一笔」、底部 + 号、首页速记框 → 同一 `NoteService`。

## 四、数据层(SQLite,重启不丢)

库:`student_workbench.db`,**version 10**(`db_helper.dart`)。版本号在 `DbHelper.dbVersion` 常量;迁移逻辑抽为静态 `runMigrations()`(便于测试驱动);**升级前 `_backupBeforeUpgradeIfNeeded` 会把旧库复制到同目录 `backups/`** 作为回滚安全网。

主要表:

| 表 | 关键字段 | 用途 |
|---|---|---|
| `tasks` | title, category, priority(0高1中2低), done, task_date, auto, created_at | 每日任务 |
| `notes` | content, favorite, mood, created_at | 灵感速记(合并随笔,可收藏/心情) |
| `wishes` | title, cost, redeemed, created_at | 心愿清单 |
| `coin_records` | amount(±), reason, type(task/bonus/redeem/english/ledger/workout/video), task_id, rec_date, created_at | 金币收支,按日统计上限 |
| `reviews` | review_date(UNIQUE), progress, done, problem, plan | 每日复盘 |
| `english_favs` | fav_type, title, content | 英文收藏 |
| `ledger_txns` / `ledger_settings` | type, amount, category, note, txn_date / key,value | 记账流水与设置 |
| `kitchen_items` / `workouts` / `health_settings` | — | 健康模块 |
| `memories` | kind, content, source, cloud_synced | AI 长期记忆(本地+可选云同步) |
| `diary_entries` | content, mood, entry_date, created_at, updated_at | 日记。**v10 起去掉 entry_date 的 UNIQUE:一天可多篇、无上限** |
| `plan_items` | scope(week/longterm), title, detail, done, due_date, sort_order | 本周计划 / 长期目标 |

迁移演进要点:v2 coin task_id → v3 复盘/英文/记账 → v4 notes.favorite → v5 notes.mood+健康表 → v6 memories → v7 diary_entries → v8 plan_items → **v10 diary_entries 重建去 UNIQUE**。
轻设置:`SharedPreferences`(昵称/主题/天气城市/问候语等)。

### 测试

- `test/unit_logic_test.dart` — 纯逻辑(版本比较、日期、每日内容确定性、金币上限 grantable、任务排序、提交去抖/换行兜底语义)。
- `test/db_migration_test.dart` — 用 `sqflite_common_ffi` 在桌面跑真实 SQLite,验证 v9→v10 迁移不丢数据 + 一天可多篇 + 模型 Map 往返。
- 共 28 项,`flutter test` 全过。

## 五、金币激励闭环(已实现)

- 获取:完成任务 +1 / 全部完成 +3,每日上限 `dailyCap = 10`(`coin_service.dart`)。
- 用途:心愿清单兑换(扣余额,`wish_service`)。
- 记录:全部收支进 `coin_records`,重启不丢。
- 待扩展(需求文档):运动打卡、视频跟练、英文学习、记账打卡等来源;阅读/技能连续打卡体系。

## 六、版本与更新机制

- `pubspec.yaml` `version` 字段为唯一版本源(当前 **`1.7.0+17`**),打包时打进 APK 资产。
- `AppInfo.installedVersion()` 运行时读内嵌 pubspec → 显示「版本 1.7.0」。
- **联网检查(唯一强制联网点)**:`AppInfo.checkUpdate()` 请求 `updateCheckUrl`(GitHub raw 上的 `latest.json`,形如 `{"version":"1.7.0","note":"…","apk":"…"}`),旧版本据此发现新版并显示更新说明;超时/离线降级为内置 `AppInfo.latestVersion` 兜底。
- **发版四步(缺一不可)**:升 `pubspec.yaml` version → 同步 `app_info.dart` 的 `latestVersion` 常量 → 更新 `latest.json` → 打包后复制到 `releases/chronos-X.Y.Z.apk`。新 versionCode(`+N`)必须大于旧版才能覆盖安装。
- APK 放对应 GitHub Release 附件;发布时需上传与 `latest.json` 中 apk URL 匹配的 tag(如 `v1.7.0`),否则旧版下载 404。

## 七、需求 14 模块 × 实现状态

| # | 模块 | 需求要点 | 状态 |
|---|---|---|---|
| 一 | 启动页 | 日期/星期/问候/金句/开始今天/快速记一笔 | ✅ 完成 |
| 二 | 首页仪表盘 | 任务概览·灵感速记·英语一句(双模式信息流已取消) | ✅ 完成(1.7.0 灵感卡加「写日记」入口) |
| 三 | 计划中心 | 今日清单·本周计划·长期目标 | ⚠️ 今日清单✅;本周计划/长期目标已建表(plan_items)页面待完善 |
| 四 | 每日复盘 | 进度/卡点/明日优化 | ✅ 完成(1.5.0) |
| 五 | 灵感收纳库 | 速记·标签·时间线·删除 | ⚠️ 速记/历史检索/收藏/批量删/心情✅(1.7.0 删除可撤销、长文省略);标签未做 |
| 六 | 英文积累 | 每日句/词/读/写,收藏,朗读,领币 | ✅ 完成(1.5.0 + 1.5.1 朗读) |
| 七 | ~~视觉灵感~~ | ~~每日推荐+六大分类收纳~~ | 🚫 已取消 |
| 八 | 健康管理 | 厨房秘籍·智能食谱·运动·跟练·热力图 | ✅ 1.6.0 完成 |
| 九 | 写随笔 → 日记 | 自由书写·列表·心情标记 | ✅ 合并入灵感速记;**1.7.0 独立日记支持一天多篇无上限、历史按日期分组、单篇删可撤销** |
| 十 | ~~阅读日记~~ | ~~在读书籍·书单·打卡·笔记~~ | 🚫 已取消 |
| 十一 | 零时闲话铺 | AI 聊天(语音/文字/图),当天清空 | ✅ 完成(中转站+elevenlabs+本地工具调用+记忆同步) |
| 十二 | 生活记账 | 流水·余额·分类·日历·图表·预算·打卡激励 | ✅ 完成(1.5.0;标签/分类排序未做) |
| 十三 | 游戏化激励 | 金币·心愿·打卡 | ✅ 金币+心愿+英文/记账/运动/视频打卡 |
| 十四 | 导航 | 顶导·抽屉·5Tab·快捷菜单 | ✅ 5Tab+抽屉+号快捷菜单 |
| 十五 | 系统设置 | 侧栏入口·版本号·主题切换·关于 | ⚠️ 侧栏+版本+关于+明暗主题✅;更多个性化未做 |

## 八、技术决定备忘(来自需求文档,后续批次注意)

1. 无视觉识别需求(穿搭助手已删)。
2. 天气接免费 API(需自注册 key),app 联网获取。
3. 智能食谱身体数据存 app 本地 sqlite,不存 APK 目录。
4. 闲话铺 AI 走中转站 API;语音用 elevenlabs 免费额度(**每月 20 号重置,注意时机**);不做发红包。
5. 记账纯本地,不做 AI/语音/自动记账外部服务。
6. 版本号手动维护在 `pubspec.yaml`;GitHub 仅作备份(可选)。
7. 环境:Flutter 3.47 + Android SDK 36 + Java 17(`D:\dev`)。

## 九、路线图

- **第一批(MVP)✅**:启动页 + 首页仪表盘 + 每日计划 + 金币系统 + 底部导航。
- **第二批 ✅**:复盘、灵感、英文、记账(均纯本地)。(视觉灵感、阅读已取消)
- **第三批 ✅**:健康、日记(原随笔)、闲话铺(含外部服务)。
- **优化批次(1.6.x–1.7.0)✅**:密钥本地化、数据导出、明暗主题、云端 AI 记忆;1.7.0 十项优化(见下方优化框架)。
- **待办**:本周计划/长期目标页面、灵感标签、记账分类排序等。

## 十、优化框架(每个迭代照此推进)

1. **分层归因**:问题先判断在 数据层 / 服务层 / UI 交互层 / 平台(输入法·系统)层 哪一层(如「done 无效」实为平台层)。
2. **组件下沉**:多页重复的东西(输入框/确认弹窗/空状态/去抖)一律下沉共享,单点修复。
3. **变更安全网**:任何 DB 结构变更 = 迁移代码 + 迁移测试 + 升级前备份,三者齐全才发版。
4. **体验一致性清单**:提交反馈、删除可撤销、长文截断+展开、空状态、加载态——每个新功能过一遍。

---

## 附:核心代码清单(按层,非全量)

```
lib/
├── main.dart                应用入口(竖屏锁定)
├── theme.dart               浅蓝配色 + Material 3(明暗双主题)
├── data/                    离线内容池(金句/英语/健康/心愿,日期种子)
├── models/                  StudentTask / Note / Wish / CoinRecord / DiaryEntry / ...
├── services/
│   ├── db_helper.dart       SQLite 单例(v10,含升级前备份 + runMigrations)
│   ├── base_dao.dart        泛型 CRUD 基类
│   ├── task/note/diary/wish/coin/review/settings/backup_service.dart
│   ├── app_info.dart        版本读取 / 最新版本 / 更新比较
│   └── llm/eleven/weather/supabase_sync_service.dart  联网服务
├── pages/
│   ├── splash / main_shell / home / plan / quick_note / diary
│   ├── note_history / note_detail / coin_center / review / english
│   ├── health / ledger / memory / chat / settings / api_settings ...
└── widgets/
    ├── app_text_field.dart  统一输入框(完成键+换行兜底+提交语义)
    ├── confirm_dialog.dart  统一确认弹窗(showConfirmDialog)
    ├── frosted_snack.dart   磨砂提示(showFrostedSnack / showUndoSnack)
    ├── section_card / mood_badge / add_task_sheet / random_task_sheet ...
test/
├── unit_logic_test.dart     纯逻辑单测
└── db_migration_test.dart   ffi 真库迁移 + CRUD
```
