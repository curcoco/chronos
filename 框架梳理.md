# 学生学习工作台 — 框架梳理

> 依据 `需求文档.md`(全 14 模块总蓝图)+ 当前代码现状,整理整体框架。
> 版本基线:1.4.0+7(2026-08-15,灵感速记极简优化)。纯本地优先,零网络。

---

## 一、总体定位

- 装到安卓真机的学生日常学习生活工作台(Flutter 打包 APK)。
- **离线优先**:计划 / 金币 / 灵感 / 记账等纯功能走本地存储;仅天气、AI 聊天等需外部服务。
- **分期交付**:先 MVP 骨架,再按批次补齐模块(见「路线图」)。

## 二、技术栈与分层

| 层 | 内容 |
|---|---|
| UI | `lib/pages/`(页面)、`lib/widgets/`(复用组件:SectionCard、AddTaskSheet、RandomTaskSheet) |
| 服务 | `lib/services/`:db_helper(sqflite 单例)、task_service、note_service、wish_service、coin_service、settings_service(SharedPreferences)、app_info(版本) |
| 数据 | `lib/data/daily_content.dart`:离线内容池(金句/每日英语/自动任务,按日期种子确定性选取) |
| 模型 | `lib/models/`:StudentTask、Note、Wish、CoinRecord |
| 工具 | `lib/utils/dates.dart`(日期格式化)、`lib/theme.dart`(浅蓝 Material 3 主题) |

## 三、导航框架(当前实现)

```
SplashPage(启动页)
 ├─ 「开始今天」→ MainShell
 └─ 「快速记一笔」→ QuickNotePage(灵感速记,直达)

MainShell(主框架)
 ├─ 底部 5 Tab:首页 / 计划 / +号(大一圈·浮起) / 知识 / 生活
 │    ├─ 首页:任务概览 · 灵感速记框 · 每日英语一句 · 金币卡 · 菜单按钮
 │    ├─ 计划:今日清单(手写添加 / 🎲随机任务)· 本周计划(占位)· 长期目标(占位)
 │    ├─ 知识 / 生活:静态占位页
 │    └─ +号 → QuickNotePage
 └─ 左侧抽屉(Drawer):应用信息 · 版本与更新卡片 · 导航快捷 · 系统设置入口
      └─ 系统设置页:关于 + 当前/最新版本 + 更新状态
```

- 首页/计划页右上角菜单按钮开抽屉(设置等次级入口已从首页移入抽屉)。
- 快捷灵感入口共 3 处:启动页「快速记一笔」、底部 + 号、首页速记框 → 同一 `NoteService`。

## 四、数据层(SQLite,重启不丢)

库:`student_workbench.db`,version 1,4 张表:

| 表 | 关键字段 | 用途 |
|---|---|---|
| `tasks` | title, category, priority(0高1中2低), done, task_date, auto(系统生成), created_at | 每日任务(自动 3 条 + 手写 + 随机) |
| `notes` | content, created_at | 灵感速记 |
| `wishes` | title, cost, redeemed, created_at | 心愿清单 |
| `coin_records` | amount(±), reason, type(task/bonus/redeem), rec_date, created_at | 金币收支历史,按日统计上限 |

轻设置:`SharedPreferences`(问候语 greeting)。

## 五、金币激励闭环(已实现)

- 获取:完成任务 +1 / 全部完成 +3,每日上限 `dailyCap = 10`(`coin_service.dart`)。
- 用途:心愿清单兑换(扣余额,`wish_service`)。
- 记录:全部收支进 `coin_records`,重启不丢。
- 待扩展(需求文档):运动打卡、视频跟练、英文学习、记账打卡等来源;阅读/技能连续打卡体系。

## 六、版本与更新机制

- `pubspec.yaml` `version` 字段为唯一版本源(当前 `1.2.0+3`),打包时打进 APK 资产。
- `AppInfo.installedVersion()` 运行时读内嵌 pubspec → 显示「版本 1.2.0」。
- **联网检查(唯一联网点)**:`AppInfo.checkUpdate()` 请求 `AppInfo.updateCheckUrl`(托管 `latest.json`,内容 `{"version":"1.2.0","note":"…"}`),旧版本因此能发现比自己新的版本并显示更新说明;超时/离线自动降级为内置 `latestVersion` 兜底,界面显示「无法连接更新服务器」。
- 抽屉与系统设置页:启动时静默检查 + 「重新检查」按钮;状态三态:发现新版本 / 已是最新版本 / 无法连接。
- 约定:每次发版**手动升 version 号并同步 `latestVersion` 与托管 `latest.json`**;新 versionCode 必须大于旧版才能覆盖安装(1.0.0+1 → 1.1.0+2 → 1.2.0+3)。

## 七、需求 14 模块 × 实现状态

| # | 模块 | 需求要点 | 状态 |
|---|---|---|---|
| 一 | 启动页 | 日期/星期/问候/金句/开始今天/快速记一笔 | ✅ 完成 |
| 二 | 首页仪表盘 | 任务概览·灵感速记·英语一句(双模式信息流已取消) | ✅ 完成 |
| 三 | 计划中心 | 今日清单·本周计划·长期目标 | ⚠️ 今日清单✅(手写+随机,自动排序/进度/领币);本周计划/长期目标静态占位 |
| 四 | 每日复盘 | 进度/卡点/明日优化 | ✅ 完成(1.5.0) |
| 五 | 灵感收纳库 | 速记·标签·时间线·删除 | ⚠️ 速记/历史检索/收藏/批量删除/心情标记✅;标签未做 |
| 六 | 英文积累 | 每日句/词/读/写,收藏,朗读,领币 | ✅ 完成(1.5.0 + 1.5.1 朗读) |
| 七 | ~~视觉灵感~~ | ~~每日推荐+六大分类收纳~~ | 🚫 已取消 |
| 八 | 健康管理 | 厨房秘籍·智能食谱·运动·跟练·热力图 | ⚠️ 1.6.0 完成(原型先行) |
| 九 | ~~写随笔~~ | ~~自由书写·列表·心情标记~~ | 🚫 已合并进灵感速记(可选心情) |
| 十 | ~~阅读日记~~ | ~~在读书籍·书单·打卡·笔记~~ | 🚫 已取消 |
| 十一 | 零时闲话铺 | AI 聊天(语音/文字/图),当天清空 | ❌ 未做(外部服务,后置) |
| 十二 | 生活记账 | 流水·余额·分类·日历·图表·预算·打卡激励 | ✅ 完成(1.5.0;标签/分类排序未做) |
| 十三 | 游戏化激励 | 金币·心愿·打卡 | ⚠️ 金币+心愿+英文/记账打卡✅;技能/运动打卡未做(依赖健康) |
| 十四 | 导航 | 顶导·抽屉·5Tab·快捷菜单 | ⚠️ 5Tab+抽屉✅;快捷菜单未做 |
| 十五 | 系统设置 | 侧栏入口·版本号·主题切换·关于 | ⚠️ 侧栏+版本+关于✅;主题切换未做 |

## 八、技术决定备忘(来自需求文档,后续批次注意)

1. 无视觉识别需求(穿搭助手已删)。
2. 天气接免费 API(需自注册 key),app 联网获取。
3. 智能食谱身体数据存 app 本地 sqlite,不存 APK 目录。
4. 闲话铺 AI 走中转站 API;语音用 elevenlabs 免费额度(**每月 20 号重置,注意时机**);不做发红包。
5. 记账纯本地,不做 AI/语音/自动记账外部服务。
6. 版本号手动维护在 `pubspec.yaml`;GitHub 仅作备份(可选)。
7. 环境:Flutter 3.47 + Android SDK 36 + Java 17(`D:\dev`)。

## 九、路线图

- **第一批(MVP)✅**:启动页 + 首页仪表盘 + 每日计划 + 金币系统 + 底部导航。
  - 第 2 次增量 ✅:启动页「快速记一笔」、系统设置页(版本号)。
  - 第 3 次增量 ✅:侧边栏(设置移入 + 版本/更新状态)、随机任务(优先级两方案),版本 1.2.0+3。
- **第二批**:复盘、灵感、英文、记账(均纯本地)。(视觉灵感、阅读已取消)
  - 1.5.0 ✅ 复盘/英文/记账;1.5.1 ✅ 优化;1.5.2 ✅ 聊天式布局/收藏/批量删除/任务保底。
- **第三批**:健康、写随笔、闲话铺(含外部服务,单独处理)。

---

## 附:已实现代码清单(按层)

```
lib/
├── main.dart                应用入口(竖屏锁定)
├── theme.dart               浅蓝配色 + Material 3 主题
├── data/daily_content.dart  金句/英语/自动任务池(日期种子)
├── models/                  StudentTask / Note / Wish / CoinRecord
├── services/
│   ├── db_helper.dart       SQLite 单例(4 表)
│   ├── task_service.dart    今日任务/自动生成/切换领币
│   ├── note_service.dart    灵感速记 CRUD
│   ├── wish_service.dart    心愿 CRUD/兑换
│   ├── coin_service.dart    余额/每日上限/收支记录
│   ├── settings_service.dart 问候语等轻设置
│   └── app_info.dart        版本读取/最新版本/更新比较
├── pages/
│   ├── splash_page.dart     启动页(开始今天/快速记一笔)
│   ├── main_shell.dart      5Tab + 侧边栏抽屉
│   ├── home_page.dart       首页仪表盘
│   ├── plan_page.dart       计划中心(今日清单/占位)
│   ├── quick_note_page.dart 灵感速记
│   ├── coin_center_page.dart 金币中心(心愿兑换/收支历史)
│   ├── settings_page.dart   系统设置(关于/版本)
│   ├── knowledge_page.dart / life_page.dart / placeholder_page.dart 占位
└── widgets/
    ├── section_card.dart    卡片容器 + 分类色 + 优先级标签
    ├── add_task_sheet.dart  手写添加任务弹层
    └── random_task_sheet.dart 随机任务弹层(优先级两方案)
```
