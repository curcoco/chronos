# 工作台 — 框架梳理

> 依据 `需求文档.md`(全 14 模块总蓝图)+ 当前代码现状,整理整体框架。
> 版本基线:**2.3.0+27**(2026-08,发版含闲话铺修复/健壮性/聊天增强/体验优化;功能完善阶段新增记忆系统升级、Auto Memory、Token 仪表盘、会话文件夹,DB 已到 **v17**)。纯本地优先,联网功能单列。
> 上手与发版流程见 `交接文档.md`。

---

## 一、总体定位

- 装到安卓真机的学生日常学习生活工作台(Flutter 打包 APK)。
- **离线优先**:计划 / 金币 / 灵感 / 记账等纯功能走本地存储;仅天气、AI 聊天等需外部服务。
- **分期交付**:先 MVP 骨架,再按批次补齐模块(见「路线图」)。

## 二、技术栈与分层(1.8.0 起:功能分区)

`lib/` 采用 **core(跨模块共享)+ features(按功能聚合)** 结构,修 bug 时一个模块只进一个目录:

```
lib/
├── main.dart              应用入口(竖屏锁定)
├── routes.dart            AppRoutes 全局路由统一入口(不再散落 MaterialPageRoute)
├── core/                  跨模块共享
│   ├── theme.dart         浅蓝 Material 3,明暗双主题
│   ├── config/            api_config(仅默认人设,无密钥)
│   ├── data/              离线内容池(金句/英语/健康/心愿,日期种子确定性选取)
│   ├── services/          db_helper、base_dao、settings_service、key_store、
│   │                      app_info、backup_service、update_installer、weather_service
│   ├── utils/             dates.dart 日期工具
│   └── widgets/           app_text_field、confirm_dialog、frosted_snack、overlay_toast、
│                          section_card、mood_badge、module_card、update_download_button
└── features/              按功能模块聚合(models + services + pages + widgets)
    ├── shell/             启动页、MainShell、AppDrawer、BottomNav、知识/生活 Tab
    ├── tasks/             今日清单 / 本周计划 / 长期目标(plan_items)
    ├── notes/             灵感速记(quick_note / note_history / note_detail)
    ├── diary/             日记(一天多篇)
    ├── coins/             金币 + 心愿(coin_service / wish_service / coin_center)
    ├── review/            每日复盘
    ├── english/           英文积累(含收藏、朗读)
    ├── health/            健康管理(厨房 / 食谱 / 运动 / 视频 / 热力图)
    ├── ledger/            生活记账(明细 / 日历 / 图表 / 分类)
    ├── memory/            AI 长期记忆(本地 + 可选云同步)
    ├── chat/              零时闲话铺(llm / eleven / nocturne / tavily / shopkeeper / tools 注册表)
    └── settings/          系统设置 / API 配置 / 拓展服务
```

### 共享组件约定(新代码必须复用,不要各页手写)

- `widgets/app_text_field.dart` `AppTextField` — 所有文本输入框。`onSubmit` 启用「完成=提交」并带中文输入法换行兜底;长文本编辑传 `submitOnEnter: false`。
- `widgets/confirm_dialog.dart` `showConfirmDialog(...)` — 所有确认弹窗;删除类传 `destructive: true`。
- `widgets/overlay_toast.dart` `OverlayToast` — 自建底部提示条(入场滑入淡入 / 退场滑出淡出,替代 SnackBar 动画);`frosted_snack.dart` 的 `showFrostedSnack` / `showUndoSnack` 为转发层,调用点不变。
- `widgets/section_card.dart` `SectionCard` — 卡片容器;`PriorityTag` / `categoryColor`。
- `services/base_dao.dart` `BaseDao<T>` — 「单例 + 简单 CRUD」的服务继承它(note/diary/wish/english/review 已改);复杂聚合(coin/plan/ledger/health/memory)仍各自实现。
- `routes.dart` `AppRoutes.push(...)` — 所有页面跳转统一入口。

## 三、导航框架(当前实现)

```
SplashPage(启动页)
 ├─ 「开始今天」→ MainShell
 └─ 「快速记一笔」→ QuickNotePage(灵感速记,直达)

MainShell(主框架)
 ├─ 底部 5 Tab:首页 / 计划 / 灵感速记 / 记账 / 闲话铺
 │    ├─ 首页:首次引导卡(一次性)· 天气条 · 任务卡(固定 3 条可上滑)· 金币卡 · 灵感速记 · 每日英语
 │    ├─ 计划:今日清单(取消任务/已完成禁操作)· 本周计划 · 长期目标
 │    ├─ 灵感速记:聊天式输入 + 时间轴历史(秒级)
 │    ├─ 记账:明细 / 日历 / 图表 / 分类
 │    └─ 闲话铺:Tab 为启动器,点击以全屏路由进入聊天页(底部导航隐藏、
 │         直达最近会话);会话切换在聊天页 AppBar「会话记录」,掌柜配置
 │         在「掌柜设置」(名称/头像/人设/记忆/模型/搜索/语音)
 └─ 左侧抽屉(水玻璃):应用信息 · 更新横幅 · 昵称/头像 · 系统设置(首位)· 功能模块区
```

- 侧边栏为**水玻璃效果**(整体 BackdropFilter 模糊 + 半透明),导航按钮磨砂玻璃。
- 快捷灵感入口共 3 处:启动页「快速记一笔」、底部 + 号、首页速记框 → 同一 `NoteService`。
- 所有跳转走 `AppRoutes`(routes.dart),便于将来切换 go_router。

## 四、数据层(SQLite,重启不丢)

库:`student_workbench.db`,**version 17**(`core/services/db_helper.dart`)。版本号在 `DbHelper.dbVersion` 常量;迁移逻辑抽为静态 `runMigrations()`(便于测试驱动);**升级前 `_backupBeforeUpgradeIfNeeded` 会把旧库复制到同目录 `backups/`** 作为回滚安全网。

主要表:

| 表 | 关键字段 | 用途 |
|---|---|---|
| `tasks` | title, category, priority(0高1中2低), done, task_date, auto, created_at | 每日任务 |
| `notes` | content, favorite, mood, created_at | 灵感速记(合并随笔,可收藏/心情) |
| `wishes` | title, cost, redeemed, created_at | 心愿清单 |
| `coin_records` | amount(±), reason, type(task/bonus/redeem/english/ledger/workout/video), task_id, rec_date, created_at | 金币收支,按日统计上限 |
| `reviews` | review_date(UNIQUE), progress, done, problem, plan | 每日复盘 |
| `english_favs` | fav_type, title, content | 英文收藏 |
| `ledger_txns` / `ledger_settings` | type, amount, category, note, txn_date / key,value | 记账流水与设置 |
| `kitchen_items` / `workouts` / `health_settings` | — | 健康模块 |
| `memories` | kind, content, source, cloud_synced | AI 长期记忆(本地+可选云同步) |
| `diary_entries` | content, mood, entry_date, created_at, updated_at | 日记。**v10 起去掉 entry_date 的 UNIQUE:一天可多篇、无上限** |
| `plan_items` | scope(week/longterm), title, detail, done, due_date, sort_order | 本周计划 / 长期目标 |

迁移演进要点:v2 coin task_id → v3 复盘/英文/记账 → v4 notes.favorite → v5 notes.mood+健康表 → v6 memories → v7 diary_entries → v8 plan_items → **v10 diary_entries 重建去 UNIQUE → v11 补齐 notes 缺失的 favorite/mood 列(修全新安装保存失败)**。
轻设置:`SharedPreferences`(昵称/主题/天气城市/问候语/**首次引导标记/上次备份时间**)。

### 测试

- `test/unit_logic_test.dart` — 纯逻辑(版本比较、日期、每日内容确定性、金币上限 grantable、任务排序、提交去抖/换行兜底语义)。
- `test/db_migration_test.dart` — 用 `sqflite_common_ffi` 在桌面跑真实 SQLite,验证 v9→v10 迁移不丢数据 + 一天可多篇 + 模型 Map 往返 + 记账删除/撤销 SQL 语义。
- `test/widget_input_test.dart` — AppTextField 交互语义(换行兜底/提交去抖)。
- 共 48 项,`flutter test` 全过。

## 五、金币激励闭环(已实现)

- 获取:完成任务 +1 / 全部完成 +3,每日上限 `dailyCap = 10`(`features/coins/services/coin_service.dart`)。
- 用途:心愿清单兑换(扣余额,`wish_service`)。
- 记录:全部收支进 `coin_records`,重启不丢。
- 扩展来源:运动打卡、视频跟练、英文学习、记账打卡。
- **任务语义(1.8.0)**:今日清单「取消任务」= 从当日移除;已完成任务不支持任何操作(不可取消完成、不可删除,金币收回路径 UI 不可达)。

## 六、版本与更新机制

- `pubspec.yaml` `version` 字段为唯一版本源(当前 **`2.3.0+27`**),打包时打进 APK 资产。
- `AppInfo.installedVersion()` 运行时读内嵌 pubspec → 显示「版本 2.3.0」。
- **联网检查(唯一强制联网点)**:`AppInfo.checkUpdate()` 请求**应用内可配置的更新源**(默认阿里云服务器 `http://120.76.230.67:18011/latest.json`,设置页可改 OSS 等),latest.json 形如 `{"version":"1.9.0","note":"…","apk":"…"}`;超时/离线降级为内置 `AppInfo.latestVersion` 兜底。
- **发版四步(缺一不可)**:升 `pubspec.yaml` version → 同步 `app_info.dart` 的 `latestVersion` 常量 → 更新 `latest.json` → 打包后复制到 `releases/chronos-X.Y.Z.apk` 并上传到服务器更新源。新 versionCode(`+N`)必须大于旧版才能覆盖安装。
- APK 上传到阿里云服务器 `/opt/chronos-update/`,与 `latest.json` 的 apk URL 对应。
- 设置页更新检查区分三种状态:检查中 / 已是最新 / 无法连接(离线)。

## 七、需求 14 模块 × 实现状态

| # | 模块 | 需求要点 | 状态 |
|---|---|---|---|
| 一 | 启动页 | 日期/星期/问候/金句/开始今天/快速记一笔 | ✅ 完成 |
| 二 | 首页仪表盘 | 任务概览(固定3条可上滑)·灵感速记·英语一句·金币卡·天气条·首次引导卡 | ✅ 完成(1.8.0 引导卡 + 任务卡固定高度) |
| 三 | 计划中心 | 今日清单·本周计划·长期目标 | ✅ 今日清单(取消任务/已完成禁操作);本周计划/长期目标 PlanListView 完整可用 |
| 四 | 每日复盘 | 进度/卡点/明日优化 | ✅ 完成 |
| 五 | 灵感收纳库 | 速记·标签·时间线·删除 | ⚠️ 速记/历史检索/收藏/批量删/心情/撤销✅;标签未做 |
| 六 | 英文积累 | 每日句/词/读/写,收藏,朗读,领币 | ✅ 完成 |
| 七 | ~~视觉灵感~~ | — | 🚫 已取消 |
| 八 | 健康管理 | 厨房秘籍·智能食谱·运动·跟练·热力图 | ✅ 完成 |
| 九 | 写随笔 → 日记 | 自由书写·列表·心情标记 | ✅ 合并入灵感速记;独立日记一天多篇无上限 |
| 十 | ~~阅读日记~~ | — | 🚫 已取消 |
| 十一 | 零时闲话铺 | AI 聊天(语音/文字),会话长期保存 | ✅ 会话管理(多会话)·中转站+工具注册表(含联网搜索 Tavily)·写操作需确认·elevenlabs 语音·**聊天发图(默认模型→OCR 模型回退)**·掌柜设置页(名称/头像/人设/记忆/模型/搜索/语音)·内置助手记忆(自动提炼/相关注入/记忆面板)·外置记忆(Nocturne MCP 用户自配) |
| 十二 | 生活记账 | 流水·余额·分类·日历·图表·预算·打卡激励 | ✅ 完成(明细 Tab 分批渲染加载更多) |
| 十三 | 游戏化激励 | 金币·心愿·打卡 | ✅ 金币+心愿+英文/记账/运动/视频打卡 |
| 十四 | 导航 | 顶导·抽屉·5Tab·快捷菜单 | ✅ 5Tab+水玻璃抽屉+号快捷菜单 |
| 十五 | 系统设置 | 侧栏入口·版本号·主题切换·关于 | ✅ 侧栏(首位入口)+版本+关于+明暗主题+**6 套浅色主题配色**+**背景图(透明度/开关/全模块预览)**+API配置+上次备份时间提醒 |

## 八、技术决定备忘(来自需求文档,后续批次注意)

1. 无视觉识别需求(穿搭助手已删)。
2. 天气接免费 API(需自注册 key),app 联网获取。
3. 智能食谱身体数据存 app 本地 sqlite,不存 APK 目录。
4. 闲话铺 AI 走中转站 API;语音用 elevenlabs 免费额度(**每月 20 号重置,注意时机**);不做发红包。
5. 记账纯本地,不做 AI/语音/自动记账外部服务。
6. 版本号手动维护在 `pubspec.yaml`;GitHub 仅作备份(可选)。
7. 环境:Flutter 3.47 + Android SDK 36 + Java 17(`D:\dev`)。
8. 路由统一走 `AppRoutes`(lib/routes.dart),不加 go_router 依赖,为将来切换留接口。

## 九、路线图

- **第一批(MVP)✅**:启动页 + 首页仪表盘 + 每日计划 + 金币系统 + 底部导航。
- **第二批 ✅**:复盘、灵感、英文、记账(均纯本地)。(视觉灵感、阅读已取消)
- **第三批 ✅**:健康、日记(原随笔)、闲话铺(含外部服务)。
- **优化批次(1.6.x–1.7.x)✅**:密钥本地化、数据导出、明暗主题、云端 AI 记忆、DB 安全网、组件下沉。
- **1.8.0 批次 ✅**:今日清单任务语义、overlay 提示条、侧边栏水玻璃、首次引导卡、备份时间提醒、记账懒加载、巨型页面拆分、路由集中、功能分区重构。
- **1.9.0 批次 ✅**:闲话铺大改(会话管理/内置助手记忆/外置记忆 Nocturne/工具注册表+写操作确认)、更新源应用内可配置(阿里云)、设置页 M3、首页记一笔、去 Supabase、欢迎界面速记返回回首页。
- **待办**:灵感标签、记账分类排序、远程内容热更(基础已在 1.9.0 落地)。

## 十、优化框架(每个迭代照此推进)

1. **分层归因**:问题先判断在 数据层 / 服务层 / UI 交互层 / 平台(输入法·系统)层 哪一层(如「done 无效」实为平台层)。
2. **组件下沉**:多页重复的东西(输入框/确认弹窗/空状态/去抖)一律下沉共享,单点修复。
3. **变更安全网**:任何 DB 结构变更 = 迁移代码 + 迁移测试 + 升级前备份,三者齐全才发版。
4. **体验一致性清单**:提交反馈、删除可撤销、长文截断+展开、空状态、加载态——每个新功能过一遍。

---

## 附:核心代码清单(按功能,非全量)

```
lib/
├── main.dart                应用入口(竖屏锁定)
├── routes.dart              AppRoutes 统一路由
├── core/
│   ├── theme.dart           浅蓝配色 + Material 3(明暗双主题)
│   ├── config/api_config.dart
│   ├── data/                离线内容池(金句/英语/健康/心愿)
│   ├── services/            db_helper(v14) · base_dao · settings · key_store ·
│   │                        app_info · backup · update_installer · weather
│   ├── utils/dates.dart
│   └── widgets/             app_text_field · confirm_dialog · frosted_snack ·
│                            overlay_toast · section_card · mood_badge · module_card
└── features/
    ├── shell/               启动页 · MainShell · 水玻璃抽屉 · 底部导航 · 知识/生活
    ├── tasks/               今日清单(取消任务) · 本周计划 · 长期目标
    ├── notes/               灵感速记三页(quick_note / history / detail)
    ├── diary/               日记(一天多篇)
    ├── coins/               金币 + 心愿
    ├── review/              每日复盘
    ├── english/             英文积累
    ├── health/              健康 5 Tab(厨房/食谱/运动/视频/热力图)
    ├── ledger/              记账 4 Tab(明细/日历/图表/分类)
    ├── memory/              AI 长期记忆
    ├── chat/                零时闲话铺(llm / eleven / nocturne / tools)
    └── settings/            系统设置 · API 配置 · 拓展服务
test/
├── unit_logic_test.dart     纯逻辑单测
├── db_migration_test.dart   ffi 真库迁移 + CRUD
└── widget_input_test.dart   输入框交互语义
```
