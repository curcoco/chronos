# 学生学习工作台 — 第一批(MVP)开工单

> 给 dsh:本次只做**第一批 MVP**,做成能装到安卓真机的 APK。
> 环境已就绪:Flutter 3.47 + Android SDK 36 + Java 17,在 `D:\dev`。
> **本批不碰任何外部服务**(天气 / AI 分析 / 聊天一律不做)。

## 要做的事(5 个模块)

### 1. 启动页(Splash Screen)
- 每日打开显示:日期 + 星期、问候语「嗨,同学」(可自定义修改)、一句随机夸赞金句(副标题)。
- 点击「开始今天」进入首页。

### 2. 首页仪表盘
- 今日任务概览:展示当日待办完成进度,同步每日计划数据;按日期随机生成 3 条;完成可领金币。
- 灵感快捷速记:随时记录一句话,自动同步到"灵感专区"。
- 每日英语一句:展示一条当日英文内容。

### 3. 每日计划任务中心
- **今日清单**:自定义添加分类 / 优先级 / 文本,自动排序,完成进度统计,全部完成领金币。
- (本周计划、长期目标:本批先做**静态占位页**,不做交互。)

### 4. 金币激励系统
- 金币获取:每日任务完成、全部完成奖励;每日上限 10 枚。
- 金币用途:心愿清单兑换(可自定义添加心愿、扣金币),记录收支历史。
- 数据本地存储,重启不丢。

### 5. 移动端底部导航
- 底部 5 Tab:首页 / 计划 / **+号快捷键**(比其他按钮大一圈、微微浮起、视觉突出)/ 知识 / 生活。
- +号快键:一键直达"灵感速记"。
- (知识、生活:本批为占位页。)
- **配色:浅蓝色。**

## 技术约束
- Flutter 3.47 + Android SDK 36,目标打包 **APK**。
- 数据用本地存储(localStorage / sqflite),重启不丢。
- **本批不接网络 / 外部服务。**

## 验收标准
- 能打包出 APK,装到安卓真机。
- 启动页 → 首页 → 记计划 → 领金币 → 心愿兑换,闭环跑通。
- 数据重启不丢。

---

# MVP 补充更新(第 2 次,增量)

> 在**已完成的第一批 MVP** 基础上增量修改,不重写。项目在 `D:\dev\student_workbench`。
> 本次同样纯本地、零外部服务。

## 补充内容(3 项)

### A. 启动页:加「快速记一笔」直达入口
- 启动页除了「开始今天」,再加一个**「快速记一笔」**(或铅笔图标)快捷入口。
- 点击后**一键直达灵感速记输入页**(和底部 + 号 / 首页速记同一个输入),不进首页、不经过「开始今天」。
- 用途:用户只想打开软件随手写点东西时的快捷通道。写完存进灵感速记,可正常返回。

### B. 新增「系统设置」页(含版本号)
- 加一个**系统设置**入口(可放在首页或导航,选个合理位置,比如首页右上角齿轮,或 + 号附近)。
- 设置页包含:
  - **关于**:应用名「学生学习工作台」、一句简介。
  - **版本号显示**:从 `pubspec.yaml` 的 `version` 读取并显示(建议显示成「版本 1.1.0」这种)。
  - (其余设置项本项不做,留待后续。)
- **打包前把 `pubspec.yaml` 的版本号升到 `1.1.0+2`**(第 1 次是 1.0.0)。

### C. 新增侧边栏(Drawer),收纳设置 / 主题
- 顶部导航加一个**汉堡菜单**图标,点开展开**侧边栏(Drawer)**。
- 侧边栏收纳**次级入口**,不占底部 5 Tab,至少包含:
  - **设置**(即 B 的系统设置页)
  - **主题切换**:默认浅蓝色,可切深色模式 / 可选强调色,选择存本地、重启不丢。
  - **关于**(应用名、版本号、一句简介)
- 侧边栏设计风格与整体浅蓝 Material 3 一致。

## 技术约束
- 纯增量修改,沿用现有 sqflite/shared_preferences 本地存储,零网络。
- 版本号读 `pubspec.yaml`(Flutter 内置,无需联网)。

## 验收标准
- 启动页出现「快速记一笔」入口,点击直达灵感速记输入并成功保存一条。
- 顶部导航有汉堡菜单,点开侧边栏,能进设置/主题/关于。
- 系统设置页能进,正确显示「版本 1.1.0」;主题切换能换深色/强调色并记住。
- 重新打包出新 APK,装真机验证通过。
