# 工作台 — 第 3 次更新(在 app 内下载并安装更新)

> 在**已完成的 1.2.0** 基础上增量修改,不重写。项目在 `D:\dev\student_workbench`。
> ⚠️ **重要背景**:本项目的「更新检查」托管已就绪 —— 源码里 `lib/services/app_info.dart` 的
> `updateCheckUrl` 我已改成真实地址
> `https://chronos-update-1456265383.cos.ap-guangzhou.myqcloud.com/latest.json`(腾讯云 COS,国内直连),
> **你不要再改回 example.com,也不要改回 GitHub**。
>
> 本次目标:让「检查更新」从**只看版本号显示说明**,升级为**真正在 app 内下载并安装新 APK**。

## 托管现状(已就绪,别动)

- **更新源:腾讯云 COS**(国内直连,朋友手机不用代理)。桶 `chronos-update`(公有读私有写)。
- `latest.json`(桶根目录)内容:
  ```json
  {
    "version": "1.2.0",
    "note": "新增侧边栏与随机任务;完成/取消任务增加确认弹窗,取消完成自动收回金币",
    "apk": "https://chronos-update-1456265383.cos.ap-guangzhou.myqcloud.com/releases/chronos-1.2.0.apk"
  }
  ```
- `releases/chronos-1.2.0.apk`(桶内 releases/ 目录)已上传,直连 200、MIME 正确、无跳转(Dart HttpClient 可用)。
- 两条地址我都已 curl 验证可达。
- GitHub 仓库 `curcoco/chronos` 仅作**源码备份/镜像**,**不再作为更新源**。

## 要做的事

### A. 扩展更新检查逻辑(`lib/services/app_info.dart`)
1. `latest.json` 现在多了 **`apk` 字段**(新 APK 直链)。`_fetchRemoteLatest()` 目前只读 `version`/`note`,
   要**加读 `apk`**。
2. `UpdateStatus` 加一个 **`apkUrl`** 字段(remote 有则填,离线兜底为 null)。
3. 不联网时保持现有兜底行为(用内置 `latestVersion`,显示「无法连接」)。

### B. 下载 APK 并触发安装(核心)
- 「发现新版本」状态里,除了现有更新说明,加一个 **「下载并更新」按钮**。
- 点击后:
  1. 用 `apkUrl` 下载 APK 到 app 可写目录(如外部缓存 /files),**显示下载进度**(百分比)。
  2. 下载完成后调起系统安装器:
     - `AndroidManifest.xml` 加 `<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>`。
     - 配置 **FileProvider**:manifest 里加 provider + 新建 `res/xml/file_paths.xml` 暴露 APK 所在目录。
     - 用 `Intent(ACTION_VIEW)` + `content://` URI + MIME `application/vnd.android.package-archive` +
       `FLAG_GRANT_READ_URI_PERMISSION` 打开安装页。
     - 原生侧在 `android/app/src/main/kotlin/com/student/student_workbench/MainActivity.kt` 加一个
       **method channel**(或选一个能装 APK 的现成插件,如 open_filex 等,选最稳的)。
     - Android 8+ 首次会弹「安装未知应用」授权,让用户点允许即可,app 无法跳过(这是安卓系统限制,正常)。
  3. 边界处理:已是新版→按钮禁用;下载失败→提示可重试;用户取消安装→静默返回。

### C. 版本号
- `pubspec.yaml` 升到 **`1.3.0+4`**。
- `lib/services/app_info.dart` 里 `AppInfo.latestVersion` 离线兜底同步到 **`1.3.0`**。

### D. 产出与同步(生成文件即可,上传由小言/我负责)
- 打包新 APK(沿用现有打包流程)。
- 把新 APK 复制为 **`releases/chronos-1.3.0.apk`**。
- 更新 **`latest.json`**:`version`=`1.3.0`、`note`=本次更新说明、`apk`=
  `https://chronos-update-1456265383.cos.ap-guangzhou.myqcloud.com/releases/chronos-1.3.0.apk`。
- **不需要你 push 或上传**,文件就位即可(小言这边负责上传到 COS + 推 GitHub 备份)。

## 技术约束
- 沿用现有架构;这是 app 唯一联网点,其余保持零网络。
- INTERNET 权限已在 manifest(别删)。

## 验收标准
- 打包出 `1.3.0` APK,`latest.json` 与 `releases/chronos-1.3.0.apk` 生成就位。
- `flutter analyze` 无报错。
- 真机:设置页/抽屉「检查更新」显示「已是最新版本」(连上真实 latest.json)。
- 真机验证下载安装链路(可用临时伪造更高版本 latest.json 触发「下载并更新」,确认能下载、进度条、弹系统安装界面)。
