name: student_workbench
description: "工作台 - 学生日常计划与金币激励应用(第一批 MVP)"
publish_to: 'none'

version: 1.2.0+3

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.1
  path: ^1.9.0
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # 运行时读取版本号:设置页「关于」展示「版本 1.1.0」
    - pubspec.yaml
