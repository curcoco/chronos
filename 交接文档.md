# Chronos(学生学习工作台)交接文档

> 交接给下一个 AI。项目根目录 `D:\dev\student_workbench`,需求依据 `需求文档.md`,框架参照 `框架梳理.md`。

## 一、项目是什么

学生日常学习生活的本地工作台(Android APK):计划、记录、激励、健康、记账、AI 聊天一体。
**设计铁律**:浅蓝极简、无 emoji、纯本地优先;联网功能(天气/闲话铺/记忆同步/更新检查)单独存在。

## 二、当前状态

- **最新已打包(可分发)**:1.6.1+13(`releases/chronos-1.6.1.apk`,02:24 重建)——**已替换**旧的 1.6.1(旧包内含已泄露的旧 key,已作废)。
- 新 1.6.1 内容:全部 API 功能(闲话铺/天气/记忆同步)+ **金币每日上限修复 + 密钥本地化(API 配置页,应用内填 key,包内无密钥)+ 日记模块**。
- `latest.json` 已指向新 1.6.1(说明已更新)。
- **使用前提**:装机后在「系统设置 → API 配置」粘贴 key(中转站/elevenlabs/心知天气/Supabase);天气城市在首页天气条应用内选。
- 后续再改代码时,按发版约定打 1.6.2+14(versionCode 递增)。

## 三、已实现模块(需求文档 15 项对照)

| 模块 | 状态 |
|---|---|
| 启动页 | ✅ 日期/星期/文艺时段问候(晨光熹微等)+昵称条件显示/28 句文艺金句/开始今天/快速记一笔 |
| 首页 | ✅ 天气条(心知天气,应用内选城市)·金币卡·今日任务·速记卡(输入+发送一行)·每日英语·头像按钮 |
| 计划中心 | ⚠️ 今日清单✅(手写/随机、保底 3 条、删除弹窗两选、仅剩 3 条→系统随机+自选优先级);本周计划/长期目标仍占位 |
| 每日复盘 | ✅ 进度/卡点/明日方案 + 历史 |
| 灵感速记 | ✅ 聊天式布局、心情标记、收藏(置顶/只看收藏)、左滑删除、历史页(搜索/日期筛选/编辑模式批量删)、详情页(分享/收藏/删除)、**日记**(长文本) |
| 英文积累 | ✅ 每日句/词/读/写、收藏、朗读(flutter_tts)、打卡+2(截断) |
| 健康管理 | ✅ 厨房秘籍/智能食谱(BMI·TDEE·3 天食谱·预算预警)/运动记录/视频跟练(计时器)/运动热力图 |
| 生活记账 | ✅ 流水/日历/图表/预算/分类/记一笔自动打卡+1 |
| 金币系统 | ✅ 任务+1、全完成+3、英文+2、记账/运动/视频+1,日上限 10(截断);心愿(添加/随机/兑换);收支历史;取消完成收回 |
| 零时闲话铺 | ✅ AI 聊天(中转站,本地工具调用:天气/今日任务/灵感)、elevenlabs 语音、人设可改、每天清空、AI 长期记忆(本地+Supabase 可选同步) |
| 导航/侧边栏/设置 | ✅ 5Tab+号、右滑热区 160、用户资料(昵称)、更新横幅(仅发现新版显示,点击下载)、API 配置页 |
| 已取消 | 🚫 视觉灵感、阅读日记、双模式信息流(需求文档已标注) |
| 未做 | 本周计划/长期目标、灵感标签、快捷菜单、主题切换、记账标签/分类排序、聊天发图/视频/表情包 |

## 四、技术栈与架构

- Flutter 3.47 / Dart ^3.13,Android SDK 36(compileSdk 36,minSdk 24),Java 17(`D:\java`)。
- 依赖:sqflite、path、shared_preferences、path_provider、share_plus、flutter_tts、audioplayers。
- 分层:`lib/pages`(页面)、`lib/widgets`(SectionCard/ModuleCard/MoodBadge/FrostedSnack 等)、`lib/services`(业务)、`lib/models`、`lib/data`(离线内容池)、`lib/config/api_config.dart`(仅剩默认人设)、`lib/utils/dates.dart`。
- **DB v7**(`db_helper.dart`,onUpgrade 逐版迁移):tasks、notes(mood/favorite)、wishes、coin_records(task_id)、reviews、english_favs、ledger_txns、ledger_settings、kitchen_items、workouts、health_settings、memories、diary_entries。
- 联网点:更新检查/下载、天气(心知天气)、闲话铺(中转站+elevenlabs)、记忆云端同步(Supabase)——其余零网络。

## 五、密钥与 API 现状(重要)

- **密钥已改为应用内填写**:系统设置 → API 配置(`api_settings_page.dart`),存 SharedPreferences(`key_store.dart`)。`api_config.dart` 已清空,只有 `defaultPersona`。
- 需用户在真机上填:中转站地址/Key/模型、elevenlabs Key/语音ID、心知天气 Key、Supabase 地址/anon key;天气城市在首页天气条应用内选。
- Supabase 建表 SQL:`supabase-记忆表.sql`(memories 表 + RLS 匿名读写)。
- **安全**:旧 1.6.1(已作废,已替换)曾含已泄露的 key;提醒用户轮换所有涉及 key;GitHub 备份前确认 `lib/config`、`lib/services/key_store.dart` 等无明文(当前源码已无任何 key)。

## 六、构建环境须知(下一个 AI 必读)

- 构建命令需要 **danger-full-access 权限**(沙箱会拦截 flutter/gradle 子进程),当前会话已获批,新会话需重新申请。
- 构建:`flutter analyze && flutter build apk --release`(JAVA_HOME=D:\java,ANDROID_HOME=D:\dev\android-sdk)。
- **gradle.properties 代理(127.0.0.1:7897)已注释**(VPN 关闭时 TLS 握手失败;直连可用)。恢复代理需取消注释。
- 环境怪癖:gradle daemon 日志常为 0 字节但实际在跑;构建耗时 10-25+ 分钟且有安静期;不要误判为卡死。
- 发版约定:升 `pubspec.yaml` version → 同步 `app_info.dart` 的 `latestVersion` → 更新 `latest.json` → 打包后复制到 `releases/chronos-X.apk`(versionCode 必须递增)。
- 已知非阻塞警告:share_plus/flutter_tts 的 KGP 警告(未来 Flutter 兼容性,暂不影响)。
- **更新下载链路(COS)目前 403 走不通**(需自定义域名+备案+CDN,用户已决定暂缓);代码已就绪,更新检查正常,下载功能待源就绪后可用。

## 七、下一步建议

1. 真机验证 1.6.1:闲话铺(API 配置后聊天/朗读/工具调用/记忆提炼+云端同步)、天气选城市、日记读写、金币上限(凑满 10 后再打卡应只发剩余额度)。
2. 按用户需求继续:本周计划/长期目标、快捷菜单、主题切换等;或优化现有交互。
3. 后续任何代码改动 → 按发版约定打 1.6.2+14。

## 八、关键文件速查

- 需求:需求文档.md / 框架梳理.md(第二批/第三批原型:第二批模块-原型.html 已删视觉与阅读)
- 配置:`lib/config/api_config.dart`(无密钥)、`lib/services/key_store.dart`
- 数据:`lib/services/db_helper.dart`(v7)、各 service
- 联网:`llm_service`(中转站+工具调用)、`eleven_service`、`weather_service`、`supabase_sync_service`、`app_info`(更新)
- 构建产物:`releases/`(chronos-1.6.1.apk 已作废)、`app-release.apk`、`latest.json`
