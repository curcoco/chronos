# Chronos(学生学习工作台)交接文档

> 交接给下一个 AI。项目根目录 `D:\dev\student_workbench`。
> **上手顺序**:先读本文件「零、最短上手路径」→ 再看「六、构建环境须知」→ 需要背景时查 `需求文档.md` / `框架梳理.md`。

---

## 零、最短上手路径(下一个 AI 只需照做)

1. **确认状态**:`git status` + 读本文件「二、当前状态」,知道最新版本号与已打包产物。
2. **改代码前**:用 `read` 看目标文件,遵守「设计铁律」(浅蓝极简、无 emoji、纯本地优先)与「四、架构约定」(输入框用 `AppTextField`、确认弹窗用 `showConfirmDialog`、可撤销删除用 `showUndoSnack`、简单表 CRUD 继承 `BaseDao`)。
3. **验证**:`dart analyze lib test` 必须零告警 → `flutter test` 必须全过(需 danger-full-access 权限,见第六节)。
4. **发版四步**(缺一不可,见「六」):升 `pubspec.yaml` version → 同步 `app_info.dart` 的 `latestVersion` → 更新 `latest.json` → 打包并复制到 `releases/chronos-X.Y.Z.apk`(versionCode 必须递增)。
5. **收尾**:回填本文件「二、当前状态」为新版本;把本次改动追加到「九、变更日志」。

> 交接一句话:**读交接文档 → 按约定改 → analyze+test → 发版四步 → 更新交接文档**。

---

## 一、项目是什么

学生日常学习生活的本地工作台(Android APK):计划、记录、激励、健康、记账、AI 聊天一体。
**设计铁律**:浅蓝极简、无 emoji、纯本地优先;联网功能(天气 / 闲话铺 / 记忆同步 / 更新检查)单独存在,其余零网络。

## 二、当前状态

- **最新已打包(可分发)**:`1.7.1+18`(`releases/chronos-1.7.1.apk`,54.5MB;根目录 `app-release.apk` 为同一份)。
- `latest.json` 已指向 1.7.1(下载 URL 按 `v1.7.1` tag 规则填写,发布 GitHub Release 时需上传对应 tag,否则旧版更新检查 404)。
- 1.7.1 本次内容:
  - **灵感速记页改为换行输入框**(`submitOnEnter: false`,回车正常换行,保存只走右侧发送按钮);首页速记框仍为单行「完成=提交」。
  - **本周计划/长期目标**编辑/删除由右滑改**左滑**(`plan_list_view.dart` endActionPane),与灵感速记左滑删一致。
  - **删除确认弹窗统一**:六处手写 `AlertDialog` 收敛到 `showConfirmDialog`(coin_center / memory / health / note_detail / note_history / ledger)。
  - **从 zip 恢复**:`BackupService.restoreFromZip`(覆盖前自动存 `.pre-restore` 副本、跳过 api_ 密钥),设置页「数据备份」卡新增按钮,**恢复后自动重启到启动页**。新增依赖 `file_picker: ^8.0.7`;`DbHelper` 增加 `close()`/`databasePath()`。
  - 新增 widget 交互测试 `test/widget_input_test.dart`;`flutter test` 共 31 项全过。
  - **构建变更**:`file_picker` 传递依赖要求 compileSdk>=36 —— `android/app/build.gradle.kts` 固定 `compileSdk = 36`,并在 `android/build.gradle.kts` 的 `subprojects` 里 `afterEvaluate` 把所有 Android 子模块 compileSdk 抬到 36(afterEvaluate 必须登记在 `evaluationDependsOn(":app")` 之前)。
- **使用前提**:装机后在「系统设置 → API 配置」粘贴 key(中转站 / elevenlabs / 心知天气 / Supabase);天气城市在首页天气条应用内选。

## 三、已实现模块(需求文档对照)

| 模块 | 状态 |
|---|---|
| 启动页 | ✅ 日期/星期/文艺时段问候+昵称/28 句金句/开始今天/快速记一笔 |
| 首页 | ✅ 天气条·金币卡·今日任务·灵感速记卡(含「写日记」入口)·每日英语·头像开抽屉 |
| 计划中心 | ⚠️ 今日清单✅(手写/随机、保底 3 条);本周计划/长期目标已建表(plan_items)但页面待完善 |
| 每日复盘 | ✅ 进度/卡点/明日方案 + 历史 |
| 灵感速记 | ✅ 聊天式布局、心情、收藏置顶、左滑删(可撤销)、历史(搜索/日期筛选/批量删可撤销/长文省略)、详情(分享/收藏/删) |
| 日记 | ✅ 一天多篇无上限、心情、保存/清空草稿、历史按日期分组、单篇删可撤销、只读详情看全文 |
| 英文积累 | ✅ 每日句/词/读/写、收藏、朗读(flutter_tts)、打卡+2 |
| 健康管理 | ✅ 厨房秘籍/智能食谱(BMI·TDEE·食谱·预算)/运动记录/视频跟练/热力图 |
| 生活记账 | ✅ 流水/日历/图表/预算/分类/记一笔打卡+1 |
| 金币系统 | ✅ 任务+1、全完成+3、英文+2、记账/运动/视频+1,日上限 10(截断);心愿添加/随机/兑换;收支历史;取消完成收回 |
| 零时闲话铺 | ✅ AI 聊天(中转站+本地工具调用)、elevenlabs 语音、人设可改、每天清空、AI 长期记忆(本地+Supabase 可选) |
| 导航/侧边栏/设置 | ✅ 5Tab+号、右滑热区 160、昵称、更新横幅、API 配置页 |
| 已取消 | 🚫 视觉灵感、阅读日记、双模式信息流 |
| 未做 | 本周计划/长期目标页面、灵感标签、主题切换、记账标签/分类排序、聊天发图 |

## 四、技术栈与架构约定

- Flutter / Dart ^3.13,Android SDK(`D:\dev\android-sdk`),构建 dart-sdk 在 `D:\dev\flutter\bin\cache\dart-sdk\bin`。
- 依赖:sqflite、path、shared_preferences、path_provider、share_plus、flutter_tts、audioplayers、archive、flutter_slidable;**dev**:flutter_test、flutter_lints、`sqflite_common_ffi`(桌面跑 DB 测试)。
- 分层:`lib/pages` / `lib/widgets` / `lib/services` / `lib/models` / `lib/data`(离线内容池)/ `lib/config/api_config.dart`(仅默认人设)/ `lib/utils/dates.dart`。
- **共享组件约定(1.7.0 起,新代码必须复用,不要再各页手写)**:
  - `widgets/app_text_field.dart` — 所有文本输入框。`onSubmit` 启用「完成=提交」并带换行兜底;长文本编辑传 `submitOnEnter: false`。
  - `widgets/confirm_dialog.dart` `showConfirmDialog(...)` — 所有确认弹窗;删除类传 `destructive: true`。
  - `widgets/frosted_snack.dart` `showUndoSnack(...)` — 所有可撤销的删除/清空。
  - `services/base_dao.dart` `BaseDao<T>` — 「单例+简单 CRUD」的服务继承它(note/diary/wish 已改)。复杂聚合(coin/plan)仍各自实现。
- **DB v10**(`db_helper.dart`):版本号在 `DbHelper.dbVersion` 常量;迁移逻辑抽为静态 `runMigrations()`(供测试驱动);升级前 `_backupBeforeUpgradeIfNeeded` 把旧库复制到 `backups/`。表:tasks、notes(mood/favorite)、wishes、coin_records(task_id)、reviews、english_favs、ledger_txns、ledger_settings、kitchen_items、workouts、health_settings、memories、diary_entries(v10 去 UNIQUE)、plan_items。

## 五、密钥与 API 现状(重要)

- 密钥应用内填写:系统设置 → API 配置(`api_settings_page.dart`),存 SharedPreferences(`key_store.dart`)。`api_config.dart` 只有 `defaultPersona`,**源码内无任何明文 key**。
- 需填:中转站地址/Key/模型、elevenlabs Key/语音ID、心知天气 Key、Supabase 地址/anon key。
- Supabase 建表 SQL:`supabase-记忆表.sql`。
- 安全:GitHub 备份前确认 `lib/config`、`key_store.dart` 无明文(当前已无)。

## 六、构建环境须知(下一个 AI 必读)

- **权限**:`flutter` / `gradle` / `dart analyze` / `flutter test` 都会 spawn 子进程,文件沙箱会拦截,需要 **danger-full-access**(每个新会话首次会因真实拦截触发一次审批;不要预先申请)。
- **验证命令**(dart-sdk 直连,避免 flutter 包装脚本的 aapt 探测崩溃):
  - 分析:`$env:PATH="D:\dev\flutter\bin\cache\dart-sdk\bin;$env:PATH"; dart analyze lib test`
  - 测试:`$env:PATH="D:\dev\flutter\bin;...;$env:PATH"; flutter test`
- **打包**:`$env:ANDROID_SDK_ROOT="D:\dev\android-sdk"; flutter build apk --release`,产物 `build\app\outputs\flutter-apk\app-release.apk`。
- **易踩坑**:构建成功后 PowerShell 可能报 `[exit code: 1]`——那是 flutter 把 KGP 弃用警告写进 stderr 被当成 NativeCommandError,**只要有 `√ Built ...apk` 即为成功**;share_plus / flutter_tts 的 KGP 警告为未来兼容提醒,当前不阻塞。
- **发版四步**(见「零」)务必齐全;versionCode(`+N`)必须比旧版大才能覆盖安装。
- 更新下载源(COS)历史上曾 403,更新检查正常、下载待源就绪(用户已知)。

## 七、关键文件速查

- 文档:`需求文档.md`、`框架梳理.md`、本文件
- 共享组件:`lib/widgets/app_text_field.dart`、`confirm_dialog.dart`、`frosted_snack.dart`
- 数据层:`lib/services/db_helper.dart`(v10)、`base_dao.dart`、各 service
- 联网:`llm_service`、`eleven_service`、`weather_service`、`supabase_sync_service`、`app_info`(更新)
- 测试:`test/unit_logic_test.dart`(纯逻辑)、`test/db_migration_test.dart`(ffi 真库迁移/CRUD)
- 产物:`releases/`、`app-release.apk`、`latest.json`

## 八、优化框架(每个迭代照此推进)

1. **分层归因**:问题先判断在 数据层 / 服务层 / UI交互层 / 平台(输入法·系统)层 哪一层(如「done 无效」实为平台层)。
2. **组件下沉**:多页重复的东西(输入框/确认弹窗/空状态/去抖)一律下沉共享,单点修复。
3. **变更安全网**:任何 DB 结构变更 = 迁移代码 + 迁移测试 + 升级前备份,三者齐全才发版。
4. **体验一致性清单**:提交反馈、删除可撤销、长文截断+展开、空状态、加载态——每个新功能过一遍。

## 九、变更日志(最新在上,发版后追加)

- **1.7.1+18**:灵感速记页改换行框(首页速记框仍单行提交)、本周计划/长期目标改左滑、删除确认弹窗统一 showConfirmDialog、新增「从 zip 恢复」并恢复后自动重启到启动页;依赖新增 file_picker 8.0.7,compileSdk 抬到 36;新增 widget 交互测试(共 31 项)。
- **1.7.0+17**:10 项优化(灵感速记完成键修复、日记一天多篇、清空草稿、历史分组、撤销删除、长文省略、写日记入口、空状态、DB 升级前备份、组件下沉+补测试)。DB v9→v10。
- **1.6.4+16**:灵感速记发送键、明暗切换即时刷新、导出 zip、隐藏拓展服务(云端 AI 记忆)、日记模块、计划随机生成。
- **1.6.1+13**:全部 API 功能 + 金币每日上限修复 + 密钥本地化 + 日记模块。
