# Chronos(工作台)交接文档

> 交接给下一个 AI。项目根目录 `D:\dev\chronos`。
> **上手顺序**:先读本文件「零、最短上手路径」→ 再看「六、构建环境须知」→ 需要背景时查 `需求文档.md` / `框架梳理.md`。

---

## 零、最短上手路径(下一个 AI 只需照做)

0. **先读 `AI协作记忆.md` 的「⚠️ 安全红线」**(删除/清理/系统目录类操作边界)——违反它的事一件都别做。

1. **确认状态**:`git status` + 读本文件「二、当前状态」,知道最新版本号与已打包产物。
2. **改代码前**:用 `read` 看目标文件,遵守「设计铁律」(浅蓝极简、无 emoji、纯本地优先)与「四、架构约定」(输入框用 `AppTextField`、确认弹窗用 `showConfirmDialog`、可撤销删除用 `showUndoSnack`、跳转用 `AppRoutes`、简单表 CRUD 继承 `BaseDao`)。
3. **找文件**:代码已按**功能分区**组织(`lib/core/` 共享 + `lib/features/<模块>/` 聚合),修一个模块的 bug 只进一个目录。
4. **验证**:`dart analyze lib test` 必须零告警 → `flutter test` 必须全过(需 danger-full-access 权限,见第六节)。
5. **发版四步**(缺一不可,见「六」):升 `pubspec.yaml` version → 同步 `app_info.dart` 的 `latestVersion` → 更新 `latest.json` → 打包并复制到 `releases/chronos-X.Y.Z.apk`(versionCode 必须递增)。
6. **收尾**:回填本文件「二、当前状态」为新版本;把本次改动追加到「九、变更日志」。

> 交接一句话:**读交接文档 → 按约定改 → analyze+test → 发版四步 → 更新交接文档**。

---

## 一、项目是什么

学生日常学习生活的本地工作台(Android APK):计划、记录、激励、健康、记账、AI 聊天一体。
**设计铁律**:浅蓝极简、无 emoji、纯本地优先;联网功能(天气 / 闲话铺 / 记忆同步 / 更新检查)单独存在,其余零网络。

## 二、当前状态

- **最新已打包(可分发)**:`2.3.0+27`(`releases/chronos-2.3.0.apk`;根目录 `app-release.apk` 为同一份)。上一已打包可分发:`2.2.1+26`。
- **更新源已切到阿里云服务器**:`latest.json` 与 APK 放服务器 `120.76.230.67:18011`(`/opt/chronos-update/`);设置页可改更新源地址(应用内可配置,默认阿里云);同目录 `content.json` 用于远程内容热更。`latest.json` 已同步到 2.3.0。
- **2.3.0 本次内容(健壮性+聊天增强+体验)**:① **闲话铺稳定性修复**:SSE 零事件自动降级非流式、200+error 事件透出真实原因、400/422 降级去 `temperature`、中转站地址兼容根地址与完整 `/chat/completions` 端点;② **聊天增强**:消息时间戳(今天不显示,跨天 MM/dd HH:mm)、长按消息复制/翻译/重新生成、错误信息完整显示在 AI 气泡(可回溯,不落库);③ **健壮性批量修复**:日志 URL 脱敏(天气 Key 不再进日志)、14 个页面加载 ErrorView+重试、金币/任务/兑换事务化、写操作兜底、备份跨版本校验、内容热更地址兼容、会话标题 emoji 截断、删除撤销一致性补缺;④ **体验优化**:背景图时顶栏半透明、背景图固定最底层(RepaintBoundary)+模糊度滑杆 0~30、侧边栏「系统设置」移最底部、换色板颜色一致(硬编码蓝改 AppColors+ThemeData 缓存)、退出确认弹窗、主题预览改缩略小图;⑤ **文案精简**(系统设置页 13 处等);⑥ **项目改名 chronos**(代码层+物理文件夹,DB 文件名保留)。
- **2.2.1 本次内容(小优化)**:① 对话上下文长度可配置——默认 80 条(此前固定 20 条),「掌柜设置 → 能力与记忆 → 对话上下文长度」滑杆可调 **10~800 条**;② **按模型档位的 token 预算 + 预警**:「API 配置 → 聊天模型」旁新增「该聊天模型支持 1M 上下文」勾选——勾选后上下文预算 100 万 token、80 万预警;不勾选按默认档预算 20 万、16.7 万预警;超过预警线时聊天页提示一次「上下文较大,建议新开会话」;超出预算的更早对话自动压缩成摘要,不丢信息、不撑爆模型上下文窗口(`core/utils/context_budget.dart`)。
- **2.2.0 本次内容(体验 + 安全 + 数据)**:
  - **聊天流式输出**:文本回复改为 SSE 逐字上屏(首字秒出),生成中按钮变「停止」,可随时停止并保留已生成部分;离开/切会话自动取消进行中的请求。
  - **发图持久化(DB v14)**:图片复制到应用文档目录 `chat_imgs/` 并随消息落库,历史消息显示原图(旧数据仍显示占位);备份 zip 与恢复均纳入聊天图片。
  - **密钥进 Keystore**:全部 API 密钥改存 Android Keystore 加密存储(flutter_secure_storage),旧版 SharedPreferences 密钥首次读取自动迁移;日志/异常输出统一脱敏。
  - **联网层统一**:HttpJson 收拢 eleven/tavily/weather/llm 的 HTTP 请求(统一超时/错误体透出/脱敏日志/连接复用);中转站 400/422 自动去参数降级重试(沿用 2.1.0)。
  - **上下文压缩**:超过 20 条时先用快速模型压缩早期对话(摘要注入),长对话不丢早期信息;失败自动回退硬裁剪。
  - **记账可编辑**:点击流水可改金额/分类/备注/日期(配合 2.1.0 的左滑删除,纠错闭环)。
  - **会话清空(可撤销)**:会话记录页新增「清空记录」,一键清空某会话消息,提示条可撤销。
  - **日志与诊断**:启动日志新增「各 API 是否已配置」摘要(不含密钥);设置页新增「导出诊断包」(db+日志+设置);自动备份开关(每周,距上次备份超 7 天自动静默导出)。
  - **其它**:共享三态组件(Loading/Error/Empty)、AppTextField 提交锁定、闲话铺首次进入引导提示、掌柜设置记忆入口补充外置记忆说明、CI 工作流(analyze+test)。
- **2.1.0 内容(修复 + 优化)**:
  - **修复 · 闲话铺掌柜不回复/日志为空**:LLM 错误信息带响应体原因(模型名/Key/参数问题一眼可见);请求、响应体读取、记忆注入全部加超时,任一环节挂起都会落日志并复位,`_sending` 不再卡死;外置记忆(Nocturne)不可达 8 秒超时降级,不再阻塞聊天主流程;中转站不支持 `tools`/`max_tokens`(400/422)时自动去参数降级重试一次,保证基本对话可用;聊天全链路埋点(发送/保存/回复/失败)+ 错误即时落盘 `app_log.txt`。
  - **修复 · 发送键无响应**:闲话铺输入框改走共享 `AppTextField`(显式 text 键盘 + 「完成」动作键 + 结尾换行兜底),中文输入法按发送键即可发出,无需先收键盘。
  - **优化 · 记账可删除**:明细左滑删除(确认弹窗 + 撤销提示),删除后余额/日历/图表联动刷新。
  - **优化 · 聊天显示用户头像**:用户消息右侧显示头像(未设置显示人形占位),与掌柜左侧头像镜像对称。
- **2.0.0 内容(体验大改六项 + 聊天发图/联网搜索)**:
  - **闲话铺**:进入即全屏聊天页(底部导航隐藏,返回回到主框架)、直达最近会话(不再先弹会话列表窗口)、AppBar 右上角改为「掌柜设置」聚合页(基础设定:名称/头像;人物设定;记忆管理;默认模型:聊天/OCR;搜索服务:Tavily 默认;语音服务:elevenlabs)、掌柜名称/头像显示在聊天页(AI 气泡头像 + 欢迎语署名 + AppBar 标题)、聊天发图(图片先走聊天默认模型,不支持时自动回退 OCR 模型识别文字再对话)、新增 web_search 工具(Tavily,只读)。
  - **侧边栏**:删除「导航」区(与底部 5 Tab 重复),系统设置入口上移到资料卡正下方。
  - **主题系统**:6 套浅色主题(蓝/粉/绿/黄/黑=深灰/白=柔和白,`core/theme.dart` 色板化)、背景图(选图存文档目录、透明度 0.3~1.0、全局开关)、全模块预览页(首页/计划/速记/记账/闲话铺/侧边栏);深色模式仍用原低亮度同色系,不随色板。
  - **细节**:底部提示条描边改中性灰、无按钮时文字居中;灵感速记历史改时间轴(由新到旧、秒级 `yyyy/mm/dd hh:mm:ss`、彩色浅色节点+竖线)。
  - **数据**:全部改动在 SharedPreferences + 文件层,**DB 仍 v13 无迁移**;备份 zip 新增打包 media(头像/掌柜头像/背景图)。
- **1.9.0 内容(闲话铺大改 + 优化方向 + 二轮体验改造)**:见下段历史。
- **使用前提**:装机后在「系统设置 → API 配置」粘贴 key(中转站 / elevenlabs / 心知天气;可选:外置记忆 URL+token、**OCR 模型**、**Tavily Key**);天气城市在首页天气条应用内选。

## 三、已实现模块(需求文档对照)

| 模块 | 状态 |
|---|---|
| 启动页 | ✅ 日期/星期/文艺时段问候+昵称/28 句金句/开始今天/四入口(速记/闲话铺/记账) |
| 首页 | ✅ 首次引导卡·天气条·金币卡·记一笔快捷入口·今日任务卡(固定3条可上滑)·灵感速记卡(写日记入口)·每日英语·头像(可自定义)开抽屉 |
| 计划中心 | ✅ 今日清单(添加/随机/取消任务/已完成禁操作)·本周计划·长期目标(PlanListView 完整可用) |
| 每日复盘 | ✅ 进度/卡点/明日方案 + 历史 |
| 灵感速记 | ✅ 聊天式布局、心情、收藏置顶、左滑删(可撤销)、历史(搜索/日期筛选/批量删/长文省略)、详情 |
| 日记 | ✅ 一天多篇无上限、心情、保存/清空草稿、历史按日期分组、单篇删可撤销 |
| 英文积累 | ✅ 每日句/词/读/写、收藏、朗读(flutter_tts)、打卡+2 |
| 健康管理 | ✅ 厨房秘籍/运动记录/视频跟练(支持用户自传视频)/热力图(智能食谱已舍弃) |
| 生活记账 | ✅ 流水(分批渲染,点击可编辑/左滑可删除可撤销)/日历/图表/预算/分类/记一笔打卡+1 |
| 金币系统 | ✅ 任务+1、全完成+3、英文+2、记账/运动/视频+1,日上限 10;心愿添加/随机/兑换;收支历史 |
| 零时闲话铺 | ✅ 会话管理(多会话/改名/删除/**清空记录可撤销**)·AI 聊天(中转站+工具注册表,含联网搜索 Tavily;**流式输出,可停止**)·写操作需用户确认·elevenlabs 语音·掌柜设置页(名称/头像/人设/记忆/模型/搜索/语音)·聊天发图(默认模型→OCR 模型回退;**图片落库,历史显示原图**)·内置助手记忆(自动提炼/相关注入/记忆面板)·外置记忆(Nocturne MCP 用户自配) |
| 导航/侧边栏/设置 | ✅ 5Tab(首页/计划/灵感速记/记账/闲话铺;闲话铺 Tab 为启动器,点击全屏进入聊天页)+水玻璃抽屉(功能模块区:英文/健康/复盘/日记/金币中心,无重复导航)+头像编辑+更新源配置+上次备份时间提醒+6 套主题配色+背景图(透明度/开关/全模块预览) |
| 已取消 | 🚫 视觉灵感、阅读日记、双模式信息流 |
| 未做 | 灵感标签、记账分类排序 |

## 四、技术栈与架构约定

- Flutter / Dart ^3.13,Android SDK(`D:\dev\android-sdk`),构建 dart-sdk 在 `D:\dev\flutter\bin\cache\dart-sdk\bin`。
- 依赖:sqflite、path、shared_preferences、path_provider、share_plus、flutter_tts、audioplayers、archive、flutter_slidable、file_picker、**mcp_client**(外置记忆 MCP)、**flutter_secure_storage**(密钥加密存储);**dev**:flutter_test、flutter_lints、`sqflite_common_ffi`(桌面跑 DB 测试)。
- **目录结构(1.8.0 起,功能分区)**:
  - `lib/core/` — 跨模块共享:theme、config、data(离线内容池)、services(db_helper/base_dao/settings/key_store/app_info/backup/update_installer/weather/**http_json**)、utils(**log_sanitize**)、widgets(**status_views**)。
  - `lib/features/<模块>/` — 按功能聚合:shell / tasks / notes / diary / coins / review / english / health / ledger / memory / chat / settings,每个含 models + services + pages + widgets。
  - `lib/routes.dart` — `AppRoutes.push(context, page, {dialog})` 统一跳转。
- **共享组件约定(新代码必须复用,不要再各页手写)**:
  - `core/widgets/app_text_field.dart` — 所有文本输入框。`onSubmit` 启用「完成=提交」并带换行兜底;长文本编辑传 `submitOnEnter: false`。
  - `core/widgets/confirm_dialog.dart` `showConfirmDialog(...)` — 所有确认弹窗;删除类传 `destructive: true`。
  - `core/widgets/overlay_toast.dart` — 自建底部提示条(入场滑入淡入/退场滑出淡出);`frosted_snack.dart` 的 `showFrostedSnack`/`showUndoSnack` 为转发层。
  - `core/services/base_dao.dart` `BaseDao<T>` — 「单例+简单 CRUD」的服务继承它(note/diary/wish/english/review 已改)。复杂聚合(coin/plan/ledger/health/memory)仍各自实现。
  - 所有页面跳转走 `AppRoutes`(lib/routes.dart)。
- **DB v14**(`core/services/db_helper.dart`):版本号在 `DbHelper.dbVersion` 常量;迁移逻辑抽为静态 `runMigrations()`(供测试驱动);升级前 `_backupBeforeUpgradeIfNeeded` 把旧库复制到 `backups/`。表:tasks、notes(mood/favorite)、wishes、coin_records(task_id)、reviews、english_favs、ledger_txns、ledger_settings、kitchen_items、workouts、health_settings、memories(v14 起 kind+content 唯一索引)、diary_entries(v10 去 UNIQUE)、plan_items、chat_sessions、chat_messages(v12;**v14 加 image_path 图片落库**)、user_videos(v13)。

## 五、密钥与 API 现状(重要)

- 密钥应用内填写:系统设置 → API 配置(`api_settings_page.dart`),**2.2.0 起存 Android Keystore 加密存储**(`key_store.dart` 走 flutter_secure_storage;旧版 SharedPreferences 密钥首次读取自动迁移并删除旧值)。`api_config.dart` 只有 `defaultPersona`,**源码内无任何明文 key**。
- 需填:中转站地址/Key/聊天模型/快速模型(记忆提炼)、**OCR 模型(发图回退识别)**、elevenlabs Key/语音ID、心知天气 Key、**Tavily API Key(联网搜索)**;可选:外置记忆(Nocturne)URL/Bearer token。
- 更新源:设置页可配置(默认阿里云服务器 `http://120.76.230.67:18011/latest.json`)。
- 安全:GitHub 备份前确认 `lib/config`、`key_store.dart` 无明文(当前已无);AndroidManifest 已开 `usesCleartextTraffic`(http 更新源/Nocturne 需要,迁 https 后可移除);日志/错误输出统一经 `log_sanitize.dart` 脱敏。

## 六、构建环境须知(下一个 AI 必读)

- **权限**:`flutter` / `gradle` / `dart analyze` / `flutter test` 都会 spawn 子进程,文件沙箱会拦截,需要 **danger-full-access**(每个新会话首次会因真实拦截触发一次审批;不要预先申请)。
- **验证命令**(dart-sdk 直连,避免 flutter 包装脚本的 aapt 探测崩溃):
  - 分析:`$env:PATH="D:\dev\flutter\bin\cache\dart-sdk\bin;$env:PATH"; dart analyze lib test`
  - 测试:`$env:PATH="D:\dev\flutter\bin;...;$env:PATH"; flutter test`
- **打包**:`$env:ANDROID_SDK_ROOT="D:\dev\android-sdk"; flutter build apk --release`,产物 `build\app\outputs\flutter-apk\app-release.apk`。
- **易踩坑**:构建成功后 PowerShell 可能报 `[exit code: 1]`——那是 flutter 把 KGP 弃用警告写进 stderr 被当成 NativeCommandError,**只要有 `√ Built ...apk` 即为成功**;share_plus / flutter_tts 的 KGP 警告为未来兼容提醒,当前不阻塞。
- **发版四步**(见「零」)务必齐全;versionCode(`+N`)必须比旧版大才能覆盖安装。
- 更新下载源(COS)历史上曾 403,更新检查正常、下载待源就绪(用户已知)。

## 七、关键文件速查

- 文档:`需求文档.md`、`框架梳理.md`、本文件
- 路由:`lib/routes.dart`(AppRoutes)
- 共享组件:`lib/core/widgets/`(app_text_field、confirm_dialog、overlay_toast、frosted_snack、section_card、**status_views**)
- 数据层:`lib/core/services/db_helper.dart`(v14)、`base_dao.dart`,业务 service 在 `lib/features/<模块>/services/`
- 联网:`lib/core/services/http_json.dart`(统一客户端/SSE/脱敏)、`features/chat/services/`(llm/eleven/nocturne/**tavily**)、`core/services/weather_service.dart`;更新源 `app_info.dart`(独立实现,不经 HttpJson)、`core/data/content_updater.dart`(内容热更)
- 掌柜设置:`features/chat/pages/shopkeeper_settings_page.dart`、`features/chat/services/shopkeeper_store.dart`(名称/头像/人设 prefs)
- 主题与背景:`core/theme.dart`(`kAppPalettes` 6 色板)、`core/services/settings_service.dart`(palette/background 通知源)、`features/settings/pages/theme_preview_page.dart`(全模块预览)
- 日志:`lib/core/services/app_log.dart`(AppLog,备份 zip 会带 app_log.txt)
- 闲话铺工具:`features/chat/tools/tool_registry.dart`(新增工具在此注册)
- 测试:`test/unit_logic_test.dart`(纯逻辑+工具注册表+内容覆盖)、`test/db_migration_test.dart`(ffi 真库迁移/CRUD)、`test/widget_input_test.dart`(输入框语义)
- 产物:`releases/`、`app-release.apk`、`latest.json`(阿里云服务器更新源)、`content.json`(远程内容热更)

## 八、优化框架(每个迭代照此推进)

1. **分层归因**:问题先判断在 数据层 / 服务层 / UI交互层 / 平台(输入法·系统)层 哪一层(如「done 无效」实为平台层)。
2. **组件下沉**:多页重复的东西(输入框/确认弹窗/空状态/去抖)一律下沉共享,单点修复。
3. **变更安全网**:任何 DB 结构变更 = 迁移代码 + 迁移测试 + 升级前备份,三者齐全才发版。
4. **体验一致性清单**:提交反馈、删除可撤销、长文截断+展开、空状态、加载态——每个新功能过一遍。

## 九、变更日志(最新在上,发版后追加)

- **未发版 · 闲话铺后台生成 + 完成通知(Android 14)**:① **前台服务保活**(`android/.../ChatForegroundService.kt`,dataSync 类型):app 退后台且掌柜正生成(`_sending`)时经 MethodChannel `app/chat_service` 启动前台服务显示「掌柜正在回复…」,进程不被系统冻结、SSE 流式继续;生成完成(成功/失败/停止)统一经 `_send`/`_regenerate` 的 `finally` 停止服务;② **完成通知**:`core/services/notification_service.dart`(flutter_local_notifications v21,`initialize`/`show` 为命名参数)弹「掌柜回复了」带内容预览(点击回 app);Android 13+ 首次发送时申请通知权限;③ **设置开关**:「数据设置」新增「后台生成通知」(`settings_service.backgroundNotify`,默认开),关闭则不做后台保活/不弹通知;④ Android 配置:manifest 加 `POST_NOTIFICATIONS`/`FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_DATA_SYNC` 权限与服务声明;build.gradle 开 core library desugaring(`desugar_jdk_libs` 2.1.4,flutter_local_notifications 要求)。`dart analyze` 零告警、70 项测试全过,打包 v8a 验证原生编译通过。

- **未发版 · 联网层常驻连接池复用**:`http_json.dart` 用单例常驻 `HttpClient`(keep-alive,idleTimeout 45s)替代每次请求新建 + `close(force)`——之前每次对话都重新 TCP/TLS 握手(中转站网络慢时拖慢回复),现跨请求复用连接池。`_send`/`postBytes`/`postSse` 统一走 `_client`,不再 force close(流式取消仍会断开该次连接并回收)。`dart analyze` 零告警、70 项测试全过。

- **未发版 · 闲话铺接生图模型(GPT-image-2 等)**:① **生图配置**:`key_store` 加 `imageGenUrl/imageGenKey/imageGenModel` 键,「API 配置」页新增「生图模型(地址/Key/模型ID)」字段;② **生图调用**:`ai_provider.dart` 加 `generateImage`(OpenAI 兼容 `POST {base}/images/generations`,`imageEndpointFor` 兼容根地址/带 /v1/已带完整端点;优先解析 `data[].b64_json`、其次 `url` 下载;返回解码图片字节)与 `imageEndpointFor`(公开便于单测);③ **聊天入口**:闲话铺输入框旁新增「生成图片」按钮(画笔图标)→ 弹提示词输入框 → 调生图 → 保存到 `chat_imgs/` → 作为 assistant 图片消息显示并落库;④ **后续引用**:生成图气泡下提供「让掌柜看看这张图」按钮 → 把图作为图片消息发给掌柜(复用 `_send` 图片链路,默认模型→OCR 回退)。`dart analyze` 零告警、70 项测试全过。

- **未发版 · 提供商「从服务商添加模型」升级为列表勾选(参考 RikkaHub ModelPicker)**:提供商详情页模型区「从服务商获取模型」改为弹出**模型选择器**(`lib/features/settings/widgets/model_picker_sheet.dart`):拉取 `GET /models` 后底部弹窗展示全部模型,支持**搜索 / 勾选加入 / 全选与取消全选**、已存在的模型标「已添加」;「确定」返回选中集合合并进提供商;手动输入模型框保留作服务商未实现 /models 时的兜底。`dart analyze` 零告警、70 项测试全过。

- **未发版 · DeepSeek 推理模型 reasoning_content 回传 + 推荐提供商(2 项)**:① **思维链回传修复**(依据 DeepSeek 官方 thinking mode 文档:**思考模式默认打开**且 **携带 tools 参数的请求,历史每条 assistant 都必须回传 reasoning_content,否则 400「must be passed back」**;参考 RikkaHub `ChatCompletionsAPI` 的 assistant 消息带 `reasoning_content`):DB **v15** 迁移给 `chat_messages` 加 `reasoning_content` 列(按列存在性补齐,兼容 v12 前旧库);`ChatMessage` 模型加字段;`chat_page` 落库(`_appendMessage` 带思维链)、加载/切会话读回、`_buildContext` 的 assistant 消息带 `reasoning_content` 返回(`history` 类型扩为 `{role, content, reasoningContent}`,ContextBudget 相应放宽签名、思维链不计 token);`llm_service` 三个入口构建 messages 时 **assistant 一律带 `reasoning_content` 键(无思考给空串兜底,防缺键被拒)**,`_streamRound` 累积 reasoning 并在工具调用续轮原样回传;**非流式路径同步捕获**:`_completeRound`/`_complete` 返回 `(text, reasoning)`,SSE 零事件降级时也 yield reasoning(否则降级轮 thinking 为空→下一轮无 reasoning_content 可回传,DeepSeek 仍报错);翻译/摘要/OCR/记忆提炼等调用点 record 字面量同步。补 v15 迁移测试。**注意:旧版产生的历史消息无 reasoning_content 数据,若复用旧会话仍可能 400——测试用新会话**。② **推荐提供商模板**(非推广):`ai_provider.dart` 新增 `recommendedAiProviders`(DeepSeek/硅基流动/OpenRouter/智谱/通义/Kimi/OpenAI/Gemini/Ollama 本地,官方 OpenAI 兼容端点),添加提供商弹窗顶部「常用服务商」快捷区——点选自动预填名称+地址,Key 留空自填。`dart analyze` 零告警、70 项测试全过。

- **未发版 · 聊天图片大图 + 未保存提示 + 首次配置引导(3 项)**:① **聊天图片点开看大图并保存**:点击聊天气泡图片 → 全屏预览(`InteractiveViewer` 双指缩放)+「保存到相册」(新增 `gal` 依赖;Android 10+ 走 MediaStore 无需权限,9 及以下在 Manifest 加 `WRITE_EXTERNAL_STORAGE maxSdkVersion=28`);② **提供商详情未保存返回提示**:名称/地址/Key/启用开关/模型增删/拉取模型任一改动后返回,弹「有未保存的修改」确认,防白改;③ **首次配置引导**:全新安装且未配置聊天服务时,进入主框架弹一次「开始使用 Chronos」(说明①添加提供商②选聊天模型,一键跳提供商页;prefs 只弹一次;已配置含旧版迁移不打扰)。`dart analyze` 零告警、69 项测试全过。

- **未发版 · 速记输入体验 + 心情徽章(4 项)**:① **灵感速记取消心情选择**(记录框上方的「心情」chips 删除、保存不再带 mood;已记录的心情**显示保留**——列表/历史/详情仍是原来的「开心/平静/难过」文字标签 `MoodBadge`,日记同款);② **输入框聚焦边框改中性灰蓝**(theme `focusedBorder` 不再用主题彩色线,契合浅淡极简);③ **速记保存按钮改浅淡风**:圆形底色与输入区一致(card),纸飞机图标用主题蓝(`primaryDark`),加载转圈改灰色;④ (曾短暂改为纯彩点 `MoodDot`,用户要求回退,已恢复文字标签)。`dart analyze` 零告警、69 项测试全过。

- **未发版 · 灵感速记去心情 + 速记页不抢键盘(2 项)**:① **灵感速记取消心情标签**(输入区心情 chips、列表/历史/详情的心情徽章全部移除,保存不再带 mood;日记页心情保留;DB mood 字段与日记迁移不动);② **灵感速记页不再自动弹键盘**(输入框 `autofocus: false`,底部导航切过来是浏览场景,想写时点输入框再弹;保存后仍保持聚焦便于连写)。`dart analyze` 零告警、69 项测试全过。

- **未发版 · RikkaHub 设置移植(模型与服务 + 数据设置)**:① **多提供商管理**:新增 `AiProviders` 仓储(`core/services/ai_provider.dart`,含 Key 存安全存储);「设置 → 模型与服务 → 提供商」支持多个中转站(名称/地址/Key/启用开关/模型列表),列表搜索/添加/详情页连接测试;旧版单中转站配置(llm_base_url 等)首次启动自动迁移为「默认中转」;② **分用途选模型**:「模型」页聊天/快速/OCR/翻译/标题各自从所有提供商模型中选择(引用格式 `providerId|modelId`,`ModelRef` 解析,补 3 项单测);1M 上下文勾选移到聊天模型项下;`LlmService` 全部请求改走模型引用解析(兼容旧键兜底);③ **设置主页重组**:新增「模型与服务」分组(提供商/模型/API 配置),「数据备份」改「数据设置」并新增「聊天图片」入口(闲话铺图片查看/删除/清空);④ **API 配置页精简**:去掉中转站/模型字段(由提供商与模型页管理),只留 Tavily/语音/天气/外置记忆;⑤ **从服务商获取模型**:提供商详情页新增「从服务商获取模型」按钮(`AiProviders.fetchModels`,OpenAI 兼容 `GET {baseUrl}/models`,自动剥离 `/chat/completions` 后缀,解析 `data[].id` 去重并入模型列表;接口不支持时提示可手填)。`dart analyze` 零告警、69 项测试全过。

- **未发版 · 全局字号阶梯调小**:新增全局 `TextScaler`(`main.dart` 的 `_StepTextScaler`,经 MaterialApp builder 的 MediaQuery 注入,一处生效全 app):≥18px 大号(计时器 46/启动页 36/金额 24/标题 22 等)×0.85 明显调小;13~18px 正文线性过渡略小(14→13.6、15→14.1、17→15);≤13px 最小辅助字号保持不变(9~13 不动)。缩放逻辑抽为公开函数 `stepTextScale`,补 4 项单测(最小不变/正文略小/大号缩小/全区间单调),保证不会「大字比小字还小」,也防止再出现「返回因子而非字号」导致文字消失的回归。`dart analyze` 零告警、66 项测试全过。

- **未发版 · 推理模型思维链 + 颜色浅淡化(2 项)**:① **思维链显示**:LLM 流式解析 `reasoning_content`(DeepSeek-R1 等推理模型),与正文分开产出;聊天气泡顶部新增「思考过程」灰色折叠块,默认收起、点击展开全文(仅内存展示、不落库,普通模型无感);② **颜色浅淡化**:新增 `AppColors.primarySoft`(浅色:蓝/粉/绿/黄 = primaryLight 再往白 20%,即天蓝/樱花粉档;黑/白两套经典色保持不变直接用 primary)与 `AppColors.onPrimarySoft`(浅底前景:蓝/粉/绿/黄深字 primaryDark,黑/白白字 onPrimary);用户聊天气泡、发送按钮、全部「primaryLight→primarySoft」渐变卡(首页金币卡/记账余额卡/金币中心/健康计时/启动页/侧边栏头像/设置页图标)前景统一走 `onPrimarySoft`。`dart analyze` 零告警、62 项测试全过。

- **未发版 · 四则 UI 优化(4 项)**:① **主题/背景预览功能整体取消**:删除设置页「预览全部模块效果」按钮与 `theme_preview_page.dart`(含「下方可预览」两处文案),选背景图后直接看效果;② **侧边栏按钮真磨砂玻璃**:功能模块与「系统设置」按钮从「半透明色」升级为 `BackdropFilter` 模糊 + 圆角裁剪 + 半透明底色的独立毛玻璃块(抽屉水玻璃之上再叠一层,按钮间留 3px 间距);③ **全局跳转过渡动画**:`AppRoutes.push` 统一改「淡入 + 轻微上滑」(260ms, `Curves.easeOutCubic`),切页不再生硬,下层路由与全局背景图在过渡期间保持可见(背景图始终是最底层);④ **闲话铺输入框按钮随状态切换**:输入为空 → 右侧「+」(选图发送,移除左侧图片按钮与纸飞机图标),输入有内容(或已选待发图) → 向上箭头发送(`arrow_upward`),生成中 → 「停止」;按钮用 `ValueListenableBuilder(_ctrl)` 局部重建。`dart analyze` 零告警、62 项测试全过。

- **2.3.0+27(健壮性批量修复 + 聊天增强 + 体验优化 + 改名 chronos)**:按「同类不成熟点审计」优先级修复(全部 `dart analyze` 零告警、59 项测试全过,无 DB 结构变更):
  - **P1 日志脱敏**:`HttpJson` 三处日志行(SSE/POST/通用)的 URL 全部过 `LogSanitize.mask`——心知天气 Key 在查询参数里,此前明文进 `app_log.txt` 并随备份/诊断包导出分享(安全项)。
  - **P4 记忆提炼健壮**:`memory_extractor` 对快速模型输出做容忍解析(非 JSON/字段类型漂移记日志并静默跳过,不再抛异常打断),只取最近 40 条对话(此前全量历史每次消息都塞给快速模型);`memory_service.add` 区分唯一约束冲突(视为去重)与真实 DB 失败(上抛可诊断),`update` 补 trim+非空校验。
  - **P3 资金一致性事务化**:`coin_service` 发币(`rewardTask`/`rewardAllDone`/`rewardDaily`)、收回(`revokeAllDone`)、兑换(`redeemWish`)的「查重+写入」全部进事务;`toggleTask` 任务状态+金币变更同事务(失败整体回滚);`chat_service` 删会话/加消息两步写进事务、`restoreMessages`/note/diary/ledger 撤销恢复幂等(`ConflictAlgorithm.ignore`);`ensureDailyTasks` 查+插原子化。新增 2 项并发集成测试(并发打卡只发一次、并发兑换只成功一次)。
  - **P2 页面加载错误态**:14 个页面(home/ledger/health/review/coin_center/notes 历史/速记/diary/plan/memory/session 列表/english/shopkeeper/extension)的 `_load` 全部补 try/catch + `AppLog.e`,失败渲染 `ErrorView`(带重试),不再「永久转圈」。
  - **P5 写操作兜底**:关键写操作(任务/心愿/记账/日记/复盘/记忆/健康/会话/头像/导出恢复)统一补 try/catch + `AppLog.e` + 友好 toast;`ledger_service` 金额(正有限数+上限)/类型/分类入口校验、`wish_service` cost 校验、记账弹层金额上限;视频上传取消清理临时文件;首页 `_init`/`_reload` 兜底;`$e` 不再直接上屏。
  - **二轮补充(低优项)**:① 备份恢复跨版本校验——备份库版本高于当前 App 时拒绝恢复(否则降级打开抛错、数据库打不开);② 内容热更地址推导兼容非 `latest.json` 形态更新源(`ContentUpdater.contentUrlFor`,补 3 项单测,此前自定义更新源热更静默失效);③ 会话标题按 grapheme 截断(`characters` 依赖,emoji/ZWJ 不再切半乱码);④ API 配置页保存不再写回 `updateCheckUrl`(与设置页双写同一 key 的维护坑消除);⑤ `review_service.save` / `english_service.toggle` 事务化(消竞态唯一约束未处理异常);⑥ 删除撤销一致性补缺:心愿/记忆删除加 `showUndoSnack` 撤销。共 62 项测试全过。
  - **补丁 · 闲话铺错误展示**:请求失败/空回复/图片不支持时,报错信息改为**完整显示在 AI 气泡里**(替换「掌柜正在想…」或追加到已有部分文本后,可回溯),不再只挤在底部提示条(一长就显示不全);`_friendlyError` 截断放宽到 600 字符;错误气泡不落库,不混入后续对话上下文。共 62 项测试全过。
  - **补丁 · 消息时间戳与操作菜单**:① 每条消息气泡显示时间戳(今天 HH:mm,跨天 MM/dd HH:mm,生成中的空气泡不显示);② **长按任意消息**弹操作菜单:复制(进剪贴板)、翻译(调聊天模型,中文→英文/其它→简体中文,译文弹窗完整展示可一键复制);③ **最后一条 AI 回复**额外支持「重新生成」——删除旧回复(内存+DB)后用原上下文重新请求(图片消息保持图片路径走原链路),失败仍以错误气泡提示。共 62 项测试全过。
  - **补丁 · 时间戳规则与文案精简**:① 时间戳改为**今天的消息不显示**,跨天消息显示「MM/dd HH:mm」(当天对话界面更干净);② 精简应用内冗余解释文案(排除设置页列表小字):上下文预警→「上下文较长,已自动压缩,建议新开会话」、聊天/语音未配置→「…请先到 API 配置」、图片不支持→「该模型不支持图片,请配置 OCR 模型」、空回复→「掌柜没有回复,请稍后重试」、欢迎语去「这里的…可以」冗余、记忆页顶部说明、记账页打卡说明、首页引导卡均改简洁。③ 输入框 hint 精简:灵感速记→「记录灵感…」(此前「记录灵感,可换行;写完点右侧保存」)、日记→「写下此刻的日记…」(去「一天可记多篇」)、健康-视频常驻说明→「上传跟练视频,点击播放。」(去「添加按钮在下方」)、Tavily 未配置工具文案对齐「请先配置…」。共 62 项测试全过。
  - **补丁 · 四项体验优化**:① **背景图时顶栏半透明**(此前全透明,滚动内容裸透重叠;现顶栏可读地盖在图片上);② **侧边栏「系统设置」移到抽屉最底部**(资料卡下方让位给功能模块,删除底部冗余小字);③ **换色板颜色一致 + 刷新优化**:抽屉头部渐变、底部提示条动作色等硬编码蓝色改走 `AppColors`(跟随色板),`AppTheme` 加 ThemeData 缓存(同「亮度×色板×背景图」复用实例,减少切换重建开销);④ **系统设置页冗长解释精简**:恢复确认/完成、导出/恢复说明、自动备份、背景图、主题模式、更新源、API 配置提示、外置记忆页、主题预览页等 13 处文案全部改简洁,「云端同步」措辞改「外置记忆」。共 62 项测试全过。
  - **补丁 · 退出确认 + 背景图模糊 + 预览缩略图**:① **退出应用加确认弹窗**(主框架 PopScope 拦截返回键,「退出应用?」确认后 `SystemNavigator.pop`,抽屉开着/有子页面时返回键仍先关抽屉/返回子页);② **背景图固定最底层**:`RepaintBoundary` 隔离重绘,子页面切换不再反复重绘背景图(FileImage 缓存不重复解码);侧边栏保持既有磨砂玻璃(BackdropFilter+半透明,打开时模糊背景图与页面);③ **背景图模糊度**:`SettingsService.backgroundBlur`(0~30 高斯模糊,prefs 持久化),设置页「模糊度」滑杆与透明度同区,`main.dart` 背景图层按模糊度套 `ImageFiltered`(模糊为 0 时不套避免无谓开销);**主题预览页取消大卡片版式**,mock 按 360 宽整体缩成固定 150 高的缩略小图(`FittedBox`)。共 62 项测试全过。
  - **补丁 · 项目改名 chronos(2026-08-20)**:代码层全面改名——pubspec `name: chronos`、全部 `import 'package:chronos/...'`(85 个 dart 文件)、Android 包名/namespace `com.student.student_workbench` → `com.chronos.workbench`(MainActivity 移至 `kotlin/com/chronos/workbench/`、proguard 同步)、IDE 文件(.iml/modules.xml)同步;**数据库文件名 `student_workbench.db` 保留**(内部细节,避免数据迁移)。`dart analyze` 零告警、62 项测试全过、release 构建验证通过。**注意:① 新包名 = 新应用**,手机上旧版无法覆盖安装,需卸载重装(可用备份 zip 导出→重装→恢复);② 物理文件夹名已由小言手动改为 `chronos`(即 `D:\dev\chronos`,此前因 Gradle daemon 占用未能改名),本文档与 `AI协作记忆.md` 里的 `D:\dev\student_workbench` 路径均已同步成 `D:\dev\chronos`。
- **2.3.0+27 附 · 闲话铺 SSE 修复**:修复「API 配置正确但提示请求失败、回复为空」。根因:2.2.0 起文本回复改走 SSE 流式,而 `HttpJson.postSse` 只认单行 `data:` JSON 事件——部分中转站忽略 `stream` 参数返回普通 JSON、或把 JSON 拆成多行、或以 200+error 事件报错时,解析器一行都收不到 → 被当成「回复为空」。修复(全部在 `llm_service.dart` / `http_json.dart`):① SSE 一个事件都没收到时自动整段降级为非流式请求(回复一次性产出,保证基本对话可用);② 中转站以 200+error 事件返回错误时透出真实原因(不再吞成「回复为空」,日志可查);③ 400/422 降级重试的请求体去掉 `temperature`(此前只去 tools/max_tokens,部分模型/代理仍拒);④ 中转站地址兼容「根地址」与「完整 /chat/completions 端点」两种填法(`LlmService.endpointFor`,补 3 项单测)。共 57 项测试全过。
- **2.2.1+26(小优化)**:① 对话上下文长度可配置——默认 80 条(此前固定 20 条),「掌柜设置 → 对话上下文长度」滑杆 10~800 条;② 按模型档位的 token 预算 + 预警:API 配置页聊天模型旁新增「支持 1M 上下文」勾选(`KeyStore.llmSupports1m`,不入 allKeys),勾选 → 预算 100 万 token/预警 80 万,不勾选 → 预算 20 万/预警 16.7 万(`ContextBudget.budgetFor/warnThresholdFor`);聊天页超预警线时提示一次「上下文较大」(`_maybeWarnContext`);发送受「条数 + 档位 token 预算」双约束自动裁剪,超预算的更早对话自动压缩成摘要。新增 6 项 ContextBudget 单测。共 54 项测试全过。
- **2.2.0+25(体验+安全+数据)**:闲话铺文本回复改 SSE 流式逐字上屏,生成中按钮变「停止」(可保留部分文本),离开/切会话自动取消请求;发图复制到 `chat_imgs/` 并随消息落库(DB v14 加 `chat_messages.image_path`,历史显示原图,旧数据占位),备份/恢复纳入聊天图片;密钥改存 Android Keystore(flutter_secure_storage,旧 prefs 自动迁移),日志/异常统一脱敏(log_sanitize);联网层收拢 HttpJson(统一超时/错误体透出/连接复用,更新源除外),400/422 自动降级重试(沿用);上下文超 20 条自动压缩摘要(快速模型,失败回退硬裁剪);记账点击可编辑;会话记录页新增「清空记录(可撤销)」;启动日志记录各 API 配置摘要(不含密钥);设置页新增「导出诊断包」与「自动备份(每周)」;共享三态组件、AppTextField 提交锁定、闲话铺首次引导、掌柜设置外置记忆说明、CI 工作流。DB v13→v14,共 48 项测试全过。
- **2.1.0+24(修复 + 优化)**:修复——① 闲话铺配置完 API 掌柜不回复、使用日志为空:LLM 非 200 错误带响应体原因;请求/响应体读取/记忆注入全部加超时,挂起不再卡死 `_sending`;外置记忆(Nocturne)不可达 8 秒超时降级不阻塞聊天;中转站不支持 tools/max_tokens(400/422)自动去参数降级重试;聊天全链路日志埋点 + 错误即时落盘(「日志为空」根因=静默挂起无日志,已消除);② 闲话铺中文输入法按发送键无反应、要收起键盘才能发出:输入框改走共享 `AppTextField`(显式 text 键盘+「完成」动作键+结尾换行兜底)。优化——① 记账明细左滑可删除(确认弹窗+撤销提示,余额/日历/图表联动刷新);② 闲话铺用户消息右侧显示用户头像(未设置显示人形占位)。共 46 项测试全过。
- **2.0.1+23(补丁)**:修复闲话铺发送消息偶发「失败+黑屏」——① 用户消息持久化移入 try,DB 写失败不再卡死 `_sending`(发不出去、无回复);② 背景图透明脚手架条件与 main.dart builder 对齐(启用且存在图才透明),并在全局最底层垫主题底色,杜绝「透明脚手架+图未渲染→黑屏」;③ `_msgImages` 渲染越界兜底、`_pickImage` 异常兜底。优化:掌柜头像 26px 圆形 → 34px 圆角方形、与气泡顶对齐更和谐。共 44 项测试全过。
- **2.0.0+22(体验大改)**:闲话铺直达全屏聊天页(隐藏底部导航)+掌柜设置聚合页(基础设定/人物设定/记忆管理/默认模型/搜索服务 Tavily/语音服务)+掌柜名称头像上屏+聊天发图(默认模型→OCR 模型回退)+web_search 工具;侧边栏删导航区、设置入口上移;6 套浅色主题(色板化)+背景图(透明度/开关/全模块预览页);提示条中性描边+无按钮文字居中;灵感速记历史改秒级时间轴(彩色浅色节点);备份 zip 纳入 media(头像/掌柜头像/背景图)。DB 仍 v13,无迁移。共 44 项测试全过。
- **1.9.0+21(二轮)**:底部导航重规划(首页/计划/灵感速记/记账/闲话铺 5Tab,移除知识/生活与+号,模块入口收进侧边栏)、欢迎界面四入口、侧边栏头像编辑(上传图片)、视频跟练用户自传视频(DB v13)、舍弃智能食谱、P4 日志收集(AppLog+备份打包)、远程内容热更(ContentUpdater+ContentStore)。共 44 项测试全过。
- **1.9.0+21(首轮)**:闲话铺大改——会话管理(DB v12,取消零点清零,按会话保存+会话列表页)、内置助手记忆(RikkaHub 式:快速模型单独配置、对话后自动提炼、相关条目注入、记忆面板编辑)、外置记忆接口层(Nocturne MCP,`mcp_client` 封装 hold/breath,未配置/断线静默降级本地)、工具注册表(新增记账/加任务/完成任务/查余额/查心愿写工具,写操作弹用户确认,速记仅可读/日记无工具);优化方向——更新源应用内可配置(默认阿里云 120.76.230.67:18011,允许明文流量)、设置页 M3 对齐、首页记一笔快捷入口、API 配置页去 Supabase(删 supabase_sync_service,拓展服务页改 Nocturne 配置页)、欢迎界面速记返回回首页。共 40 项测试全过。
- **1.8.0+20**:功能分区重构(lib → core/+features/)、路由集中(AppRoutes)、巨型页面拆分(main_shell/ledger/health/home)、今日清单删除改取消+已完成禁操作、首页任务卡固定3条可上滑、自建 overlay 提示条(退场动画)、侧边栏水玻璃+导航磨砂、首页首次引导卡、设置页上次备份时间提醒、记账明细分批渲染、English/Review 服务并入 BaseDao、文档同步更新。DB v11 不变。
- **1.7.2+19**:修复全新安装用户灵感速记无法保存(`_onCreate` 漏建 notes.favorite/mood,新增 v11 迁移 `_ensureNotesColumns` 幂等补列;保存失败显式提示);底部提示不排队(showFrostedSnack/showUndoSnack 先 removeCurrentSnackBar)。DB v10→v11,补迁移测试(共 33 项)。
- **1.7.1+18**:灵感速记页改换行框(首页速记框仍单行提交)、本周计划/长期目标改左滑、删除确认弹窗统一 showConfirmDialog、新增「从 zip 恢复」并恢复后自动重启到启动页;依赖新增 file_picker 8.0.7,compileSdk 抬到 36;新增 widget 交互测试(共 31 项)。
- **1.7.0+17**:10 项优化(灵感速记完成键修复、日记一天多篇、清空草稿、历史分组、撤销删除、长文省略、写日记入口、空状态、DB 升级前备份、组件下沉+补测试)。DB v9→v10。
- **1.6.4+16**:灵感速记发送键、明暗切换即时刷新、导出 zip、隐藏拓展服务(云端 AI 记忆)、日记模块、计划随机生成。
- **1.6.1+13**:全部 API 功能 + 金币每日上限修复 + 密钥本地化 + 日记模块。

## 十、发布与开源备选方案(2026-08-20 整理,备用)

> 现状:自用 app,尚未正式对外发布;签名与更新机制已就绪,随时可以给朋友用或开源。
> 本章是「备选方案」:需要时照做,不需要时不动。**给朋友用不需要开源,开源也不等于给朋友用。**

### 10.1 给朋友用(个位数用户,最简单)
- **直接发 APK**:微信/网盘传 `releases/chronos-X.Y.Z.apk`(~57.8MB),对方直接装,零额外配置。
- 已就绪的保障:
  - **正式签名**:`android/chronos-release.jks`(build.gradle.kts 已配:有 keystore 用 release 签名,否则回退 debug)。
    每次发版签名一致 → 朋友可**直接覆盖安装**,不用卸载重装。
  - **自动更新**:把 `latest.json` + APK 传到阿里云服务器 `120.76.230.67:18011`(`/opt/chronos-update/`),
    朋友装好后「设置 → 关于 → 发现新版本」一键更新(更新源地址可在设置里改)。
- 发版流程(给朋友前):升 pubspec version → 同步 `app_info.dart` → 更新 `latest.json` → 打包复制到 `releases/`
  (见「零、五」发版四步)。

### 10.2 GitHub 公开(可选,想要「开源项目」形态才做)
- 前置:改名 chronos 后的改动**一次都没 commit**;仓库 `github.com/curcoco/chronos` 目前是 private + 旧代码。
- 步骤:
  1. `git add .` + commit(先确认工作区无敏感文件)。
  2. 加 `LICENSE`(自用项目推荐 MIT / Apache-2.0)。
  3. 重写 `README.md`(当前正文还是 Flutter 模板,只改了标题)。
  4. README 注明「设置界面设计参考 RikkaHub(AGPL-3.0)」——Chronos 是 Kotlin→Flutter 完全重写、
     未复制其代码,借鉴 UI 布局不构成 AGPL 衍生,但注明出处最稳妥。
  5. 仓库设置 private → public。
- 已确认安全(可直接 push):
  - `.gitignore` 已排除 `key.properties` / `*.jks`(签名与密码不进仓库,`git ls-files` 验证无跟踪)。
  - API 密钥运行时填写、源码无明文;若有改动复查 `lib/config` 与 `key_store.dart`(见「五、安全」)。
  - `latest.json` 里的更新源地址 `120.76.230.67:18011` 是个人服务器,公开无妨;介意可改成环境配置。

### 10.3 其他注意
- APK ~57.8MB;微信/网盘可传,邮件附带有上限。
- 新用户无数据,无需备份引导;老用户升级前仍建议导出备份(zip 含数据库/图片/设置)。
- 若将来用户数增长:更新源已有;需再考虑崩溃上报/渠道包(当前无)。
